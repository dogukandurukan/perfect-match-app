# Perfect Match — CLAUDE.md

Bu dosya, repo + terminal erişimi olan agent'ları projeye hızlı ve doğru şekilde tanıtmak içindir. Her oturumun ortak zeminidir.

**Repo:** `github.com/dogukandurukan/perfect-match-app` · Yerel: `~/dating-app-recovered`
**Aşama:** Alpha, MVP öncesi (~%75–80). Tek geliştirici (Doğukan), part-time ~10–15 saat/hafta.
**Supabase proje ref:** `fyqwjduzpnjuxqsloxih`

---

## 0. ÖNCE BUNU OKU — kritik kurallar (agent'ların düştüğü tuzaklar)

1. **RPC ve RLS policy'leri git'te DEĞİL, Supabase içinde yaşıyor.** `get_top_matches` (eşleştirme motoru) ve tüm Row Level Security politikaları veritabanında. Bir davranışı kodda bulamıyorsan DB'ye bak; grep RPC gövdesine giremez — `SELECT pg_get_functiondef('public.get_top_matches'::regproc);` ile çek.
2. **İSİMLE değil ID ile ilerle.** Seed'de 13 tane "Asli" var. Kullanıcı ayrımını her zaman UUID ile yap, `first_name` ile asla.
3. **DB'ye yazan değişiklikten önce constraint/policy kontrol et.** Kod doğru olsa da DB reddedebilir (örn. `matches.status` için `matches_status_check`). RLS bir insert'i sessizce engelleyebilir.
4. **Bir katmanı elemeden diğerine geçme.** Teşhis sırası: (a) şikayeti grep'le (`--exclude-dir=node_modules --exclude-dir=.git`), (b) kodda yoksa DB/RPC'yi tara, (c) constraint/policy'yi doğrula. Tahminle katman atlama.
5. **Supabase sorgu hataları sessizce yutuluyor.** Çoğu yerde `.single()`/`.maybeSingle()` sonucunun `error`'ı loglanmıyor. Bir ekran "boş" görünüyorsa büyük ihtimalle select hata veriyordur (var olmayan **tek** bir kolon tüm select'i null yapar). Şüphelendiğinde `const { data, error } = ...` yapıp error'ı logla.
6. **Değişiklikleri küçük ve kapsamlı tut: test → commit → sıradaki.** İstenmeyen refactor / import düzeni / stil temizliği yapma.

**Çalışma tarzı (geliştiricinin tercihi):** adım adım ilerle — tek problem bul, göster, fix et, dur. Kapsamlı tara ama tek seferde her şeyi değiştirme. Ajanları gerektiğinde kullan ama context'i yorma.

---

## 1. Proje nedir

Skor bazlı otomatik eşleştirmeye dayalı bir **dating uygulaması**. Klasik "sonsuz swipe" yerine iki-adımlı, niyetli tanışma akışı.

- **Çekirdek farklılaştırıcı (henüz kurulmadı):** Hinge tarzı **bağlamlı beğeni** — bir profilin belirli öğesine (foto/soru) beğeni. `likes` tablosu planlı; asıl ürün kimliği burada olacak.
- **İkinci tez:** "Yavaş dating" — davet → kabul → sohbet → buluşma (check-in) funnel'ı; hız yerine gerçek buluşma.
- **UI referansı:** Hinge. Altın accent `#B8860B` (`colors.accent`).
- **Kuzey-yıldızı metrik:** **buluşma oranı** (match → chat → gerçekten buluştu mu). Hem ürün farkı hem en değerli veri.

Çekirdek döngü uçtan uca doğrulanmış (like → davet → kabul → chat → mesaj).

---

## 2. Teknoloji ve ortam

**Stack:** Expo + React Native + TypeScript + `expo-router` + Supabase (Postgres + Auth + Storage + Realtime).
**Görüntü:** `expo-image`. **Realtime chat:** Supabase channel `postgres_changes` INSERT aboneliği.
**Çalışma modu:** **dev-client** (2026-08-26'da Expo Go'dan göçüldü — **push notification artık ÇALIŞIYOR**, uçtan uca test edildi). `eas.json` var, EAS projesi + Apple credentials zaten kuruluydu (Haziran'dan kalma). Doğukan'ın telefonuna kurulu development build var; **Asli'nin cihazı henüz provisioning profile'da yok** (ayrı `eas device:create` + yeni build gerekir).

### Çalıştırma
```bash
cd ~/dating-app-recovered
# Artık Expo Go DEĞİL, kendi dev-client'ın (telefonda "dating-app / Development Build" ikonu):
npx expo start --dev-client -c
```
- Telefonda dev-client açık değilse önce elle aç, "Enter URL manually" ile Metro adresini gir ya da QR tara.
- **Kod değişince:** `r` = reload (saf JS/TS fix'lerde yeter). **Yeni bir native modül eklenirse** (örn. bugün `@react-native-community/datetimepicker`'ın ilk kullanımı) dev-client'ın YENİDEN BUILD edilmesi gerekir (`npx eas build --profile development --platform ios`), sade reload yetmez — Expo Go'dan farklı, dev-client kendi içine derlenmiş native kod taşıyor.
- **Push test/debug:** `npx supabase functions deploy send-push-notification --project-ref fyqwjduzpnjuxqsloxih` (kaynak: `supabase/functions/send-push-notification/index.ts`, git'te). CLI önce `npx supabase login` ister (tarayıcıdan onay; ara sırada çıkan macOS Keychain penceresi zararsız, **Deny** diyebilirsin). Dashboard'ın Edge Function kod editörüne büyük kod yapıştırmak bozulmalara yol açtı (özel karakter/otomatik-parantez sorunu olabilir) — **CLI ile deploy güvenilir, Dashboard-paste değil.**
- **Eski not (artık geçerli değil, referans için):** Expo Go modunda tünel (`--tunnel`) gerekiyordu çünkü Asli'nin telefonu LAN'da bağlanmıyordu; dev-client'a geçince bu durum yeniden değerlendirilmeli.

### DB / migration uygulama (DDL) — DİKKAT
- **`supabase db push` KÖR ÇALIŞTIRMA.** Remote şema geçmişi CLI-tracked değil (geçmiş değişiklikler SQL editor'dan elle yapıldığından 8 yerel migration remote'ta "unapplied" görünüyor). Push, migration geçmişini önce onarmadan güvenli değil.
- **Tek dosyalık DDL uygula:** `supabase db query --linked -f <dosya>` (CLI bu makinede authenticated). Ya da Supabase Studio → SQL Editor'a yapıştır.
- **Supabase MCP bağlantısı bu ajan oturumlarında salt-okunur** → DDL/yazma CLI ister. (Örnek: `likes` tablosu 2026-08-16'da `db query --linked -f` ile canlıya alındı.)
- **Yeni kolon eklendikten sonra PostgREST şema önbelleği bayat kalabilir** (2026-09-10'da bulundu, `quick_icebreaker_answers` kolonuyla). Ham SQL migration (`db query --linked -f`) Dashboard/normal migration akışından geçmiyor — PostgREST (client'ın kullandığı REST katmanı) yeni kolonu görmeyip `select`/`update`'i PGRST hatasıyla reddedebiliyor. Kod tarafında error hiç loglanmıyorsa (CLAUDE.md kural #5) bu tamamen sessiz kalır — "yazıyor gibi görünür ama DB'de hep null kalır" şeklinde ortaya çıkar, saatlerce yanlış yerde debug ettirebilir. **Yeni kolon ekleyince hemen ardından `NOTIFY pgrst, 'reload schema';` çalıştır** (SELECT gibi salt-okunur bir komut, MCP'den de gider) — hem de o kolona dokunan her select/update'e `error` logu ekle, "sessiz kaldı" ihtimalini bir daha kaçırma.

### Test hesapları (ID ile ilerle!)
| Kişi | UUID |
|---|---|
| Doğukan | `e5426159-0d49-42c5-b79d-0d1b3cdfea9a` |
| Asli (gerçek 2. hesap) | `1d2a579f-434c-4392-9556-974c8b2617e0` |

---

## 3. Mimari

### Çekirdek döngü
```
Home (keşif) ──like──▶ Matches ("Let's meet" davet)
                          ├─ karşı taraf kabul ──▶ chat açılır ──▶ mesajlaşma
                          └─ confirmed meetups ──▶ check-in
```
Funnel iki-adımlı ve iki cinsiyette **simetrik** (bkz. §7 ürün kararları).

### Ekran haritası (`app/`)
| Dosya | Rol |
|---|---|
| `(tabs)/index.tsx` | Home — keşif kartları (`get_top_matches` RPC), like |
| `(tabs)/matches.tsx` | Matches — davet gönder/al, açık sohbetler, pass |
| `(tabs)/messages.tsx` | Konuşma listesi |
| `(tabs)/notifications.tsx` | Alerts |
| `(tabs)/map.tsx` | Bölge yoğunluk haritası (rapor "ÖLÜ" diyordu ama route aktif — silmeden önce teyit et) |
| `(tabs)/vibe.tsx` · `(tabs)/profile.tsx` | Vibe akışı · kendi profil |
| `chat.tsx` | Birebir sohbet — realtime, icebreaker, chat-gate |
| `user-profile.tsx` | Karşı tarafın profili + block/report |
| `premium.tsx` | Monetizasyon iskeleti — artık Profile içindeki banner'dan açılıyor, global header'da değil (2026-09-06) |
| `settings.tsx` | Account/Notifications/Privacy/App — discovery tercihleri artık burada değil, `filters.tsx`'te |
| `filters.tsx` | Discovery tercihleri — şehir, ilişki tarzı (intent), kimi görmek istediğin, yaş aralığı, mesafe (2026-09-06, yeni; `settings.tsx`'ten ayrıldı) |
| `checkin.tsx` · `micro-intro.tsx` | Buluşma check-in · mikro tanışma |

### Önemli lib helper'ları (`lib/`)
- `matchInvite` → `orderedPair(a, b)` — iki kullanıcıyı deterministik sıraya sokar (user_a_id / user_b_id).
- `quickIcebreaker` → `QUICK_ICEBREAKER_QUESTIONS`, `buildQuickIcebreakerLine(answers)` — chat'teki 5-soruluk this-or-that quiz (eski profil-kıyaslama `icebreakers.ts` silindi, 2026-09-09).
- `labels` → `formatIntentLabel`, `formatDrinkingLabel`, `formatSmokingLabel`, `formatAvailabilityLabel`.
- `designTokens` → `colors` (accent `#B8860B`), `spacing`, `radius`. **Borç:** çoğu ekran gri tonlarını ve `ACCENT`'i hardcode ediyor; `border/textMuted/bgSubtle` eklenmeli.
- **Foto URL için İKİ ayrı helper var, karıştırma:**
  - `resolveProfilePhotoUrl` → `getProfilePhotoPublicUrl(path)` — **public** URL (chat başlığı, mesaj avatarları).
  - `userPhotosStorage` → `resolveProfilePhotoUrl(path, 3600)` — **signed** URL, 1 saat (user-profile ekranı).
  - Storage bucket: `user-photos` (public).

### Veri modeli — anahtar tablolar
Kod referansına göre kullanılan tablolar: `profiles`, `matches`, `onboarding_answers`, `messages`, `notifications`, `blocks`, `reports`, `venues`, `match_pair_scores`.

**`profiles`** (DB'den doğrulanmış kolonlar — dikkat: **`intent` YOK**):
```
availability_days, availability_hours, bio, city, core_value, country_code,
created_at, current_step, daily_invites_count, daily_invites_reset_at,
daily_views_count, daily_views_reset_at, date_of_birth, dealbreaker, deleted_at,
dial_code, discovery_age_max, discovery_age_min, discovery_max_distance, district,
drinking, education, education_detail, expo_push_token, favorite_activity,
favorite_book, favorite_movie, favorite_music, favorite_spots,
first_date_expectation, first_name, full_address, gender, hide_location, hobbies,
id, impressed_by, is_hidden, languages, last_name, lat, lng, meeting_environment,
meeting_preferences, morning_night, neighborhoods, notify_meeting_invite,
notify_messages, notify_new_match, occupation, pets, phone_number, photo_verified, photos,
preferred_locations, recharge_style, religion, setup1_completed, setup_completed,
smoking, updated_at, username, vibe, zodiac_sign
```
**`matches`** — `id, status, chat_opened, match_score, user_a_id, user_b_id, created_at, expires_at, updated_at`. `status` ∈ `pending | accepted | expired | passed` (constraint `matches_status_check`). `expired` = süre doldu; `passed` = elle geçildi (bilinçli olarak ayrı).
**`messages`** — `id, sender_id, receiver_id, content, created_at`
**`onboarding_answers`** — niyet/kişilik verisi. **`intent` verisi burada** (profiles'ta değil). KVKK açısından gözden geçirilecek.
**`blocks`** — `blocker_id, blocked_id` · **`reports`** — `reporter_id, reported_id, reason`
**`likes`** — **UYGULANDI (2026-08-16, CLI ile canlıya alındı)** (`supabase/migrations/20260816120000_create_likes.sql`); tablo + 3 RLS policy birebir. Eski uyumsuz tablo (`liked_id`, target_type/note/status yok, 0 satır) drop edildi. **Client Home'da BAĞLI** (❤ + Note tek `recordLike` upsert). **Likee-okuma DA BAĞLI (2026-08-18)** — `get_my_likers` SECURITY DEFINER RPC (`supabase/migrations/20260817100100_get_my_likers_rpc.sql`, CLI ile uygulandı) + `profiles.is_premium` stub kolon (`20260817100000_profiles_is_premium.sql`); Buzz `notifications.tsx` bu RPC'den besleniyor. Dondurulmuş şema (Home bağlamlı-beğeni + Buzz Faz B tek tablo):
```sql
create table likes (
  id          uuid primary key default gen_random_uuid(),
  liker_id    uuid not null references profiles(id) on delete cascade,
  likee_id    uuid not null references profiles(id) on delete cascade,
  target_type text not null check (target_type in ('photo','prompt','profile')),
  target_key  text,          -- foto path/index VEYA prompt kolon-anahtarı; 'profile' iken null
  note        text check (char_length(note) <= 240),   -- opsiyonel Bumble "Note"; ücretsiz yazılır
  status      text not null default 'sent'
              check (status in ('sent','matched','passed','expired')),
  match_id    uuid references matches(id) on delete set null,  -- ön-sinyal → davet bağı
  created_at  timestamptz not null default now(),
  unique (liker_id, likee_id)   -- MVP: kişi başına tek hedefli beğeni (şema çok-reaksiyona gevşetilebilir)
);
```
RLS/premium (Faz B): likee her zaman **count** görür; `note`/`target_key`/kimlik yalnız premium (Buzz unlock) → RPC blur'lu vs full döndürür.

### RPC — `get_top_matches` (Supabase içinde, git'te yok)
Eleme penceresi:
```sql
m.status IN ('pending', 'accepted')
OR (m.status = 'expired' AND m.created_at > now() - interval '14 days')
OR (m.status = 'passed'  AND m.created_at > now() - interval '42 days')
```
Pencere `created_at`'e bakıyor (pass anına değil); kesin "pass'ten N gün" istenirse `updated_at`'e çekilmeli. `not_sure_yet` matching ağırlığı (20, normalizer 150) sert olabilir — açık karar. Diğer RPC'ler: `try_send_invite`, `upsert_match`.

### RLS modeli (denetlendi)
- **messages:** INSERT sadece sender isen ve accepted match varsa (simetrik); SELECT/UPDATE sadece sender/receiver. Eski `messages_own` bypass'ı kapatıldı.
- **matches:** tek insert policy `matches_insert_own`, `with_check (auth.uid() = user_a_id OR auth.uid() = user_b_id)`. Gevşek `matches_insert_policy` silindi.
- ~~🔴 **Kalan not (beta öncesi)**~~ — **anon sızıntısı KAPANDI (2026-08-29, `2396929`, migration `20260829090000`).** `profiles_select_all`/`onboarding_select_all` rol `public` + `qual true` idi — Postgres'te "public" anon (login'siz) isteği de kapsıyor; anon key app bundle'ında herkese açık olduğu için login olmadan tüm kullanıcıların `phone_number`/`date_of_birth`/`full_address`/`lat-lng`'i + onboarding cevapları çekilebiliyordu (curl ile doğrulandı — gerçek kullanıcı satırı döndü). Aynı şekilde `notifications_insert` public+`with_check:true` idi, herkes herhangi bir kullanıcının Buzz'ına sahte bildirim yazabiliyordu. Fix: profiles/onboarding SELECT → `authenticated`'e çekildi (login'li discovery akışı bozulmadı — client kodu grep'lendi, hiçbir yerde anon/pre-login profil okuma yok); `notifications_insert` policy'si tamamen kaldırıldı (client hiç insert etmiyor, tüm yazmalar Edge Function service-role ile zaten RLS bypass ediyor). Fix sonrası anon curl ile 3'ü de doğrulandı (profiles/onboarding boş `[]`, insert `42501` ile red). ~~**Kalan (ayrı, daha büyük iş, ertelendi):** `is_hidden`/`deleted_at` hâlâ RLS seviyesinde değil~~ — **BU DA KAPANDI (2026-08-29, `2c8ef9f`, migration `20260829093000`).** `get_top_matches` (Home RPC) SECURITY DEFINER DEĞİL → profiles RLS'e tabi; önceki fix her satırı koşulsuz döndürdüğü için gizli/silinmiş profil hâlâ **yeni keşif kartı olarak strangers'a sızabiliyordu** (asıl risk buydu, salt "biri sorgu atarsa görür" değil). Fix: `profiles_select_authenticated` artık `auth.uid()=id OR (is_hidden=false AND deleted_at IS NULL) OR EXISTS(matches satırı var)` — gizli/silinmiş profil yalnızca zaten `matches` satırı olan tarafa görünüyor (mevcut chat/Matches/Buzz bozulmadı), yeni keşifte hiç çıkmıyor. `onboarding_answers` bilinçli olarak dokunulmadı (get_top_matches'te profiles zaten filtrelenince join otomatik düşüyor, ayrıca tek başına first_name/foto içermiyor — düşük risk).

---

## 4. Güncel durum & açık işler

**Bitmiş (son oturumlar):**
- Chat → profil navigasyonu + block/report erişimi; pass→'passed' (3 katman senkron); RLS güvenlik denetimi (2 açık kapatıldı); sol (karşı taraf) mesaj avatarları.
- **`intent` bug FİX'Lİ** — `user-profile.tsx` artık `intent`'i `profiles` select'inden çıkarıyor ve ayrı `onboarding_answers` sorgusuyla çekip profile'a birleştiriyor ("Looking for" satırı geri geldi).
- **Ortak `ErrorState` bileşeni** (`components/ErrorState.tsx`) — index/messages/notifications/map ekranlarına retry ile bağlandı (gerçek hata artık boş listeden ayırt ediliyor).
- **chat.tsx** — `chatOpened===null` iken spinner (kilit-ekranı flaş'ı giderildi); gönderme hatası artık görünür (inline uyarı + metin geri konur).

**Bitmiş (2026-08-15):**
- ✅ **Sağ (kendi) mesaj avatarı** — `chat.tsx`'e `myPhotoUrl` + `myInitial` state + `profiles(photos, first_name)` fetch eklendi; `renderMessage`'ın `isMine` dalı balonun sağında avatar gösteriyor (sol tarafın simetriği). Commit `fa060e1`.
- ✅ **Balon avatarına tıklayınca profile git** — sol (theirs) son-balon avatarı `TouchableOpacity` ile header'ın `openUserProfile()`'ına bağlandı. Commit `2481e30`.
- ✅ **Error state audit TAMAMLANDI** — ortak `ErrorState` retry'lı olarak tüm ana ekranlara bağlandı: `vibe` (`77def58`), `user-profile` (`35faf25`), `matches` (`77b9cec`), `chat` gate (`a944308`). Desen: `error`+`reloadKey` state, fetch `try/catch`, kısmi içerik varsa korunur (`error && !hasContent`). Daha önce index/messages/notifications/map zaten bağlıydı. rn-ui-reviewer (built-in `claude`+sonnet ile) vibe & matches'i review etti; matches'te kritik bir bulgu (myMatches error yutulması) düzeltildi.

**Bitmiş (2026-08-16):**
- ✅ **Kalan sessiz-yutulan select hataları kapandı** — matches `profiles`(by id) davet/sohbet listesi hatası artık error state'e taşınıyor; `intentRows` (yardımcı veri) `console.warn` ile loglanıp bloklamadan degrade oluyor (`52984bf`). chat `fetchMessages` hatası → `messagesError` state + `ErrorState` retry, kısmi içerik korunur (`a468d59`). **Error-state serisi tamamen bitti.**
- ✅ **chat klavye boşluğu giderildi (cihazda doğrulandı)** — `ScreenContainer` alt safe-area padding'i chat'te 0'landı, alt inset input satırına taşındı (klavye kapalıyken `insets.bottom`, açıkken 12; `Keyboard` show/hide listener). `keyboardVerticalOffset` sabit 90 → `0` (bu düzende `behavior='padding'` offset'i düz boşluk olarak koyuyor; boşluk ≈ offset). Commits `3c9418f`+`3ca3602`.
- ✅ **chat input bar cila** — send butonu 40→44pt; `'↑'` glyph → `Ionicons arrow-up` + gönderirken `ActivityIndicator`; send+back butonuna `accessibilityRole/Label/State`; `designTokens`'e `bgSubtle/border/textMuted` eklendi ve chat input hardcode grileri + `radius.pill` token'a geçti (`33cd58d`). Setleri `phosphor` yerine mevcut `@expo/vector-icons`/`Ionicons` ile yaptık.
- ✅ **chat foto ekleme butonu — UI iskeleti (P2 Katman 1)** — `expo-image-picker` eklendi; input solunda kamera butonu (44pt, nötr dolgu, `camera-outline`, a11y) → Alert sheet (Take Photo / Choose from Library) → izin + picker açılıyor; seçim sonrası şimdilik **"Coming soon"** (upload YOK, bilinçli) (`95ab34e`).

**Bitmiş (2026-08-16, devam):**
- ✅ **Alerts → "Buzz" sekmesi: premium likes redesign (A→B fazlı, Faz A).** Ürün kararı: A→B fazlı — önce backend'siz premium kabuk, sonra ayrı oturumda gerçek likes backend. **Sekme "Alerts" → "Buzz"** (`_layout.tsx` title+headerTitle; ekran başlığı "Buzz"). `notifications.tsx` üç bölge (kullanıcı feedback v3 — sıralama: **önce featured, sonra feed, EN ALTTA likes footer**): (1) **`FeaturedCard`** (ÜSTTE, `ListHeaderComponent`) — `invite_accepted` ("X said yes") + `new_invite`/`meeting_invite` ("X wants to meet") yüksek-sinyal: 52pt avatar + rozet (checkmark yeşil / cafe altın) + kalın başlık + renkli aksiyon pill ("Pick time"/"Respond"), gölgeyle öne çıkar. (2) **Compact feed** (ORTA) — mesaj/check-in/expiry `Ionicons` tint rozet satırları ("Earlier" başlığı). (3) **`LikesSection`** (EN ALTTA, `ListFooterComponent`) — "N people like you" + **4'lü blur STRIP** (3 blur'lu yüz tile + 4. "+N" sayaç tile; `expo-image` `blurRadius:22`, yeni bağımlılık YOK; `resolveProfilePhotoUrl` signed URL, foto yoksa baş-harf fallback; her tile'da faint heart) + full-width davetkâr **"Unlock to see who likes you"** butonu → `/premium`; likeCount 0 iken null. Zone sınıflandırması tek yerde: `LIKE_TYPES`/`FEATURED_TYPES` + `isLikeType`/`isFeaturedType`; `LIKE_FACE_TILES=3`; zone'lar `useMemo`. Faz B bağı: `LikesSection` count/avatars kaynağı → B'de `likes` tablosuna geçecek, premium'da unblur. tsc temiz. **Commit edilmedi; cihazda görsel doğrulama sürüyor.**

**Not (2026-08-16):** `messages` sekmesi de yeniden adlandırıldı: **"Messages" → "Chats"** (`_layout.tsx` title+headerTitle + `messages.tsx` ekran başlığı). Görünen alt bar: Home · Matches · Buzz · Chats (Vibe/Map/Profile/explore zaten `href:null`, bardan gizli ama erişilebilir; Profile sağ-üst avatardan).

**Bitmiş (2026-08-17/18):**
- ✅ **Home Bumble redesign + likes write-side** (`4bba4d9`, 2026-08-17) — interleave scroll profil, Note bottom-sheet composer, About-me/Looking-for/Interests/Languages/Location chip sistemi, verified rozeti; Home ❤+Note tek `recordLike`→`likes` upsert.
- ✅ **Buzz Faz B — READ side TAMAMLANDI** (`a7a448d`) — `profiles.is_premium` stub + `get_my_likers` SECURITY DEFINER RPC (`auth.uid()` bazlı, non-premium'da kimlik/not NULL). `notifications.tsx` `LikesSection` artık `notifications` değil bu RPC'den besleniyor: locked'da anonim kalp tile (eski client-side blur gerçek fotoyu sızdırıyordu, düzeltildi), unlocked'da gerçek foto/isim + tıklayınca `user-profile`. Tek kişi likete isim başlıkta ("X likes you"). Cihazda locked+unlocked doğrulandı. Aşağıdaki "🔜🔜 Home redesign" ve eski "🔜 Alerts Faz B" kalemleri bu ikisiyle kapandı.
- ✅ **Emoji chip'lerden Bumble tarzı vektör ikon sistemine geçiş** (`b6d053a`) — `lib/hingeProfile.ts` `ProfileChip` artık `{key, icon: Ionicons-name, label}`; About-me/Looking-for/Interests/Languages chip'lerindeki gömülü emoji (👤🎓🍷🚭🐾🕊️💎✨⭐🌈💬 vb.) kaldırıldı, tek-renk outline ikonlarla değişti. `user-profile.tsx` (Aslı'nın profili, Mayıs'tan beri dokunulmamıştı) aynı builder'lara (`buildAboutMeChips/buildLookingForChips/buildInterestChips/buildLanguageChips/buildAvailabilityChip`) + `SectionCard`/`ChipGrid`'e taşındı — Home ile aynı görsel dil. Alt aksiyon butonlarındaki (Send message/Report/Block) literal emoji de vektör ikonla değişti. Cihazda doğrulandı, kullanıcı onayladı.

**Bitmiş (2026-08-19):**
- ~~🔜 **UI-4 — rn-ui-reviewer pass + push.**~~ — TAMAMLANDI (2026-08-18). `rn-ui-reviewer` 3 kritik/yüksek bulgu buldu+düzeltti (user-profile loading spinner eksikliği, Chats CTA + user-profile aksiyon butonlarında a11y label eksikliği); `get_my_likers` RPC kimlik-sızıntısı riski kontrol edildi, sorun yok. Kozmetik bulgular (hardcode gold palet, 42pt<44pt dokunma hedefi vb.) bilinçli ertelendi. Push edildi (`994c1c7`, `origin/main`).
- ✅ **Buzz featured card — inline Respond TAMAMLANDI (`961bf4a`).** "Wants to meet" kartı artık ✕/✓ butonlarıyla Matches'e gitmeden kabul/red ediliyor (`acceptMatchInvite` reuse edildi; red = matches.tsx'teki "Maybe later" ile aynı, DB'ye yazmıyor, sadece yerel dismiss — matches.tsx da hard-decline persist etmiyor, tutarlı). Bu arada bulunan/düzeltilen 3 bug: (1) featured kartlar `is_read` filtrelemiyordu → kabul/red edilen (hatta sadece açılan) davet Buzz'a her dönüşte tekrar geliyordu, düzeltildi (`is_read` artık filtre); bu ayrıca Haziran'dan kalma 2 çürük seed bildirimini de otomatik gizledi. (2) Chat hemen açılmayan accept (`shouldOpenChatOnAccept` cinsiyet kuralı) sessizdi → onay alert'i eklendi. (3) Tek beğenide locked tile tüm satırı kaplayıp dev kare oluyordu (`flex:1`, kardeş yok) → `maxWidth:110` ile sınırlandı.
- ✅ **Handled invite'lar feed'e "demote" oluyor, yok olmuyor (`30ea6c2`).** Accept/decline sonrası kart tamamen kaybolunca Buzz "sadece likes kutusu" kalan, neredeyse boş bir ekrana dönüyordu (kullanıcı testi sonrası bulundu) — artık `is_read` olan featured item'lar compact feed'e "You said yes to X" / "You said not now to X" özet satırı olarak düşüyor (`NotificationRow.resolvedSummary`, client-only/persist edilmiyor — reload sonrası `feedRowText`'teki generic fallback'e döner, örn. "You responded to X's invite"). Cihazda doğrulandı, kullanıcı onayladı.
- **Not — Supabase CLI ağ sorunu:** Bu oturumda `npx supabase db query --linked`/`projects list` gibi tüm CLI komutları sürekli timeout'a düştü (muhtemelen dünkü ngrok SNI-block'una benzer bir ağ engeli). Yazma gereken test-veri işlemleri bu yüzden **Supabase Dashboard SQL Editor** üzerinden elle yapıldı (kullanıcı çalıştırdı). MCP bu oturumda salt-okunur kaldığı için doğrulamalar `mcp__supabase__execute_sql` ile yapıldı. Gelecek oturumda CLI'yı tekrar dene, hâlâ patlıyorsa aynı dashboard-SQL yolunu kullan.

**Bitmiş (2026-08-20):**
- ✅ **Buzz'a önerilen mekan+saat önizlemesi eklendi (`e63668d`).** "Wants to meet" featured kartı artık davetçinin `micro-intro.tsx`'te seçtiği mekan+3 saat önerisini gösteriyor (`introAnswersForUser`+`formatIntroLines`, `matches.user_a/b_intro_answers` — Matches'te zaten vardı, Buzz'da eksikti). Birkaç görsel iterasyon sonrası: 📍 mekan / 🕐 saatler ayrı satırlarda, kartın alt kısmında böl geçit çizgisiyle; feed satırları sade kaldı (sadece mekan, saatler yok — "You're chatting with X"e detay eklemek kullanıcı tarafından reddedildi, kalabalık görünüyordu). Cihazda birkaç tur test edildi, onaylandı.
- **Netleşen bilgi — "Pick time & place" akışı zaten kısmen VARMIŞ:** `micro-intro.tsx` (Matches'teki "Let's meet" ekranı) çoktan mekan (`venues` tablosu, bölgeye göre önerili) + müsaitliğe göre önerilen 3 saat seçimi yapıyor, `matches.user_a/b_intro_answers` jsonb'ye yazıyor. **Eksik olan tek şey:** kabul edildikten sonra 3 öneriden BİRİNİN kesinleşmesi — şu an yok, taraflar kesin saati muhtemelen sohbette kendi aralarında konuşuyor. `checkin.tsx` da zaten var (gidildi mi + 1-10 rating + `checkin_a/checkin_b/checkin_confirmed` karşılıklı onay) — kullanıcının sorduğu "rate verme" fikri **zaten yapılmış**, sadece Buzz'da hiç hatırlatılmıyor (Matches'in "confirmed meetups" bölümünden erişiliyor).
- **Not — is_premium test state:** Doğukan hesabı (`e5426159-...`) artık kalıcı olarak `is_premium=true` (kullanıcı isteğiyle, dashboard'dan). Gerçek ödeme sistemi yok, bu sadece stub flag. Buzz'da "seni kim beğendi" artık her zaman unlock görünecek — test ederken bunu hesaba kat.
- **Not — Supabase CLI hâlâ kırık:** Bugün de aynı ağ sorunu devam etti, tüm `npx supabase db query --linked` denemeleri timeout'a düştü (yanlışlıkla art arda macOS Keychain izin penceresi de tetikledi — zararsız, Cancel'lanabilir). Dashboard SQL Editor yolu bugün de kullanıldı. Yarın önce hızlıca `npx supabase db query --linked "select 1;"` ile dene, 10-15sn'de dönmezse direkt dashboard'a geç, uzun süre CLI ile uğraşma.

**Açık:**
- ~~🔜 **"Pick time" — kesin saat/mekan onayı**~~ — TAMAMLANDI (`3289258`, 2026-08-21). Davet kabul edilirken invitee artık 3 önerilen saatten birini chip olarak seçmek ZORUNDA (seçmeden ✓ pasif) + "Suggest another time" ile serbest metin karşı-önerisi yapabiliyor; mekan için de "Suggest a different place" linkiyle aynısı (opsiyonel, varsayılan davetçinin önerisi). `matches.confirmed_slot`/`confirmed_place` (text, migrations `20260821090000`/`093000`) — kabul anında yazılıyor, davetçinin "said yes" kartında "Confirmed: ..." olarak görünüyor, CTA "Open chat"a dönüyor. **Bilinçli tek taraflı:** sadece invitee kesinleştiriyor, davetçinin ayrıca onaylaması gerekmiyor (checkin.tsx'teki karşılıklı `checkin_a/checkin_b` deseni burada KULLANILMADI — basit tutuldu, taraflar zaten sohbette konuşabilir). Yol boyunca bulunan/düzeltilen 2 bug: (1) kartın tamamı tek büyük `TouchableOpacity` olduğu için TextInput'a dokunmak bile kartı "okundu" işaretleyip kaybediyordu — kabul edilmemiş kartlar artık düz `View`, sadece ✕/✓ karar veriyor. (2) kabul sonrası özet metni hem geçici (ephemeral) hem kalıcı (DB fallback) kaynaktan aynı bilgiyi ekleyip tekrarlı görünüyordu — artık tek kaynak (`matches.confirmed_slot`). Cihazda custom saat+mekan ile test edildi, onaylandı. **Not:** test verisinde Derya match'inde (`bdcbeba8-...`) gerçek olmayan "Kadikoy bina? / Friday evening 20?" değerleri duruyor, temizlenmedi (zararsız, sonra silinir).
- ~~🔜 **Buzz "activity history" feed**~~ — TAMAMLANDI (`b179eb0` görsel + `bd0e989` veri katmanı, 2026-08-21). Görsel: `FlatList`→`SectionList`, feed artık Bugün/Dün/Bu hafta/Daha önce diye tarihe göre gruplanıyor. Veri: `likes` (liker_id=ben) satırları da senkron feed'e giriyor ("You liked X's photo/answer"), demoted-invite + like-sent ikonları tek sıcak-altın tona (`colors.accent`, outline, aynı krem bg) birleştirildi (önceki yeşil-tik/kırmızı-kalp karışımı kullanıcıya "güzel değil" geldi). `invite_accepted` artık feed'e hiç düşmüyor (kullanıcı istemedi — "Pick time"/"Open chat" sonrası Chats'e taşınıyor zaten). Feed metni artık mekanı da içeriyor ("You said yes to X — Friday evening at Kronotrop"). Cihazda doğrulandı.
**Bitmiş (2026-08-24/25):**
- ✅ **Blocker #1 çözüldü — gerçek tarih/saat altyapısı (`d22999a`, `54e0d68`, `b6ad332`).** `micro-intro.tsx` artık soyut "Saturday afternoon" yerine gerçek takvim tarihi öneriyor (müsaitlikten hesaplanan yaklaşan tarihler) + kurulu `@react-native-community/datetimepicker` ile tam özel tarih/saat seçimi (ilk gerçek kullanımı). `lib/matchInvite.ts`: `suggestMeetingTimes()` (ISO üretir) + `formatMeetingTime()` (24h format, "17:00") + `formatIntroLines()` eski etiket-metni verilerle geriye uyumlu. Kesinleşen saat artık **`matches.meeting_at`** (zaten var olan, hiç kullanılmayan gerçek `timestamptz` kolonu) içine yazılıyor — eski metin-tabanlı `confirmed_slot` bırakıldı. Yol boyunca cihazda bulunan/düzeltilen 3 bug: (1) saat aralığı etiketleri ("Afternoon (12-18)") aralığın başına göre çevriliyordu → hep "12 PM" çıkıyordu, ortalamaya çevrildi. (2) mekan önerisi mantığı hem senin hem karşı tarafın `district`'ine bakacak şekilde genişletildi ama `venues.district` Türkçe karakterli ("Beşiktaş"), `profiles.district` karaktersiz ("Besiktas") olduğu için hiç eşleşmiyordu — diyakritik normalizasyonu eklendi (mahalle-seviyesi değerler — "Moda" gibi — hâlâ eşleşmiyor, ayrı bir iş, not düşüldü). (3) iOS spinner picker + onay butonu aynı satırda paylaşılınca buton ekran dışına taşıyordu (picker kendiliğinden sarmıyor) → picker üstte, buton altında tam genişlikte ayrı satıra alındı (hem micro-intro.tsx hem notifications.tsx'te). Mekan öneri etiketi "Near them" yerine gerçek isim gösteriyor ("Near Melis"). Cihazda uçtan uca test edildi (gerçek davet gönder → Buzz'da kabul et → gerçek tarih kaydedildi), kullanıcı onayladı.
- ~~🔜 **Blocker #2 — push bildirim altyapısı**~~ — TAMAMLANDI (2026-08-26, `85bf2f1`). **Büyük keşif:** client-side push kaydı (`lib/notifications.ts`+`app/_layout.tsx`) ve bir Supabase Edge Function (`send-push-notification`) + `matches` tablosuna bağlı webhook (`matches-push-notification`) **zaten yazılmıştı** — hiç test edilmemişti çünkü Expo Go SDK 53+ remote push desteklemiyor. Bugün: `eas.json` eklendi, taze iOS dev-build alındı (Apple credentials zaten kurulu — Haziran'dan kalma), telefona kuruldu, `--dev-client` ile Metro'ya bağlandı. Push token kaydı + gerçek bildirim teslimatı cihazda doğrulandı. Edge Function eski/kullanılmayan alanlara (`user_a_accepted`, her INSERT'te ateşleme — Home'un discovery-card üretimi bile match satırı yaratıyor, gerçek davet değil) bakıyordu, güncel `lib/matchInvite.ts` akışıyla eşleşecek şekilde yeniden yazıldı (sadece `invited_by` yeni set edildiğinde + `chat_opened` false→true olduğunda push) ve **artık repoda** (`supabase/functions/send-push-notification/index.ts`, CLI ile deploy). match_score push metnine ekleniyor ("— %78 uyum!"). **Kalan:** Asli'nin cihazı provisioning profile'da yok, app ikonu placeholder (bildirimde sarı kutu).
- ~~🔜 **Gün-içi buluşma hatırlatma bildirimi**~~ — TAMAMLANDI (2026-08-27, `401ac5b`). `supabase/functions/send-meetup-reminders/index.ts` (yeni, git'te) + `pg_cron` job `daily-meetup-reminders` — **saatlik** çalışıyor, `meeting_at <= now()` (geçmiş, 24h lookback) + `meetup_reminder_sent_at IS NULL` filtresiyle: buluşma saatinden kısa süre sonra hem push hem `notifications` satırı (`type='meetup_reminder'`) yazıyor. Push tıklaması `checkin.tsx`'e deep-link (`app/_layout.tsx`, soğuk-başlatma dahil — `getLastNotificationResponseAsync`+`clearLastNotificationResponseAsync`, warm-app `addNotificationResponseReceivedListener`'ın kaçırdığı case). **Buzz'da da kalıcı kart:** `notifications.tsx`'in `FeaturedCard`'ına üçüncü dal (`meetup_reminder`) — ✕ direkt `matches.checkin_a/checkin_b=false` yazıp feed'e demote ediyor, ✓ `checkin.tsx`'e `wentThere=1` ile atlayıp rating adımına gidiyor (`checkin.tsx`'teki `skipToRating`). Matches tablosuna `meetup_reminder_sent_at` (migration `20260827090000`). **Bulunan/düzeltilen 2 bug:** (1) cron ilk halde sabit 06:00'da tek seferde "bugün" filtresiyle çalışıyordu → akşam buluşmalarında "gittiniz mi" (geçmiş zaman) sorusu daha buluşma olmadan geliyordu, saçma duruyordu (cihaz testinde bulundu) — saatlik + "geçmiş" filtresine çevrildi. (2) Buzz'daki ✕/✓ ilk testte "Match not found" verdi — kök neden ayrı: test match'i (`bdcbeba8-...`) haftalar önce `expired` olmuş kalıntı test verisiydi (`expire-matches` cron'u yalnız `pending` olanları expire ediyor, bugünkü kodla ilgisizdi), gerçek bug değildi. Cihazda uçtan uca doğrulandı (push tetikleme, cold-start deep-link, Buzz ✕/✓), kullanıcı onayladı.
- 🔜 **Asli'nin cihazı için provisioning** — `npx eas device:create` ile UDID kaydı + yeni bir development build (mevcut build sadece Doğukan'ın telefonunu kapsıyor).
- 🔜 **App ikonu placeholder** — bildirimlerde ve ana ekranda hâlâ sarı/gold bir kutu (`assets/images/icon.png`), gerçek bir logo tasarlanmadı.

**Bitmiş (2026-08-29/30) — güvenlik + kapsamlı denetim + profil tutarlılığı + hatırlatma iki-aşamalı:**
- ✅ **İKİ GÜVENLİK AÇIĞI KAPANDI (`2396929`, `2c8ef9f`).** (1) `profiles_select_all`/`onboarding_select_all` rol `public` + `qual:true` idi → anon key ile (login'siz, sadece app bundle'ındaki herkese açık key ile) tüm kullanıcıların `phone_number`/`date_of_birth`/`full_address`/`lat-lng`'i çekilebiliyordu — curl ile doğrulandı (gerçek satır döndü), sonra fix sonrası tekrar doğrulandı (boş döndü). `notifications_insert` de aynı şekilde açıktı, herkes herhangi bir kullanıcının Buzz'ına sahte bildirim yazabiliyordu. SELECT → `authenticated`'e çekildi, insert policy'si kaldırıldı (client hiç insert etmiyor, Edge Function'lar service-role ile zaten bypass ediyor). (2) `get_top_matches` SECURITY DEFINER DEĞİL → profiles RLS'e tabi; gizli/silinmiş profil hâlâ **yeni keşif kartı olarak strangers'a sızabiliyordu**. Fix: `profiles_select_authenticated` artık `auth.uid()=id OR (is_hidden=false AND deleted_at IS NULL) OR EXISTS(matches satırı var)` — mevcut chat/Matches/Buzz bozulmadı, yeni keşifte hiç çıkmıyor. Migrations `20260829090000`/`20260829093000`.
- ✅ **Kapsamlı kod+DB denetimi yapıldı** (bir subagent ile `app/`+`lib/` statik tarama + benim DB/RPC/RLS/constraint/index/trigger taramam). Sonuçlar aşağıda "Açık"a P0/P1/P2 olarak işlendi. Bulunan/kapanan 3 P0: (1) `profile-setup/step4.tsx` var olmayan `/profile-setup/success`'a yönlendiriyordu → **her yeni kullanıcı onboarding bitince buna çarpıyordu**, `/(tabs)`'a düzeltildi. (2) `profile-setup/step1.tsx`'teki "Use my location" sabit Kadıköy koordinatı yazıyordu (state default'uyla aynıydı, fiilen hiçbir şey yapmıyordu) → gerçek `expo-location` GPS + reverse-geocode'a geçirildi. (3) Foto yükleme hatası sessizce yutulup 0 fotoyla kaydediliyordu → artık hata gösterip kaydetmeyi durduruyor. Commit `6ac329e`.
- ✅ **Onboarding+profil foto limiti 3 → 5** (`step1.tsx` `PHOTO_SLOTS`, `profile-edit.tsx` `MAX_PHOTOS`) — kullanıcı isteği, tek satır/yatay scroll layout otomatik sığıyor, native değişiklik yok.
- ✅ **Kendi Profile ekranı Home ile tutarlı hale getirildi** (`eb8198c`). `(tabs)/profile.tsx` artık Home'u besleyen `HingeProfileCard`'ı kullanıyor (About me / Things in common / I'm looking for / interests / prompt kartları), eski elle yazılmış ayrı layout kaldırıldı. `HingeProfileCard`: "Things in common" emoji'si kaldırıldı, müzik/film/kitap satırları vektör ikona geçti, hobi chip'leri About-me ile aynı dolu-krem stile alındı (önceden ayrı altın-kenarlıklı stildi — kullanıcı raporu). `profile-edit.tsx` tamamen İngilizceye çevrildi + aynı ikon sistemi + **"My ideal date" eklendi** + **"My go-to spot" gerçekten çalışıyor hale geldi**. Bunu yaparken bulunan bug: onboarding'in buluşma-ortamı etiketleri (`"A coffee date ☕"` vb.) ile profile-edit'inki (`"Coffee"` vb.) **farklı string'lerdi** → `get_top_matches`'in ortam-örtüşme puanlaması sessizce kırılıyordu (biri profilini düzenleyince). `lib/meetingVenues.ts` ortak dosyaya çıkarıldı, `step4.tsx`+`profile-edit.tsx` artık aynı kaynaktan besleniyor.
- ✅ **Home'da like/dislike Bumble deseni** (`eb8198c`). X/❤ butonları artık kartın EN ALTINDA (önceden "Things in common"dan hemen sonra, ortadaydı); altına **Block**/**Report** eklendi (Home'dan direkt, önceden sadece `user-profile.tsx`'te vardı) — Block onay diyaloğuyla `blocks`'a yazıp bir sonraki profile geçiyor, Report alttan açılan sheet'le sebep alıp `reports`'a yazıyor, ikisi de hatada artık sessiz kalmıyor (uyarı gösteriyor — user-profile.tsx'teki sessiz-hata deseni tekrarlanmadı). Süper like yok (istenmedi). **Bulunan/düzeltilen bug:** Report sheet'inde `KeyboardAvoidingView` unutulmuştu, klavye açılınca sheet klavyenin arkasında kalıp görünmez oluyordu (cihaz testinde bulundu) — Note composer'daki ile aynı desene alındı.
- ✅ **Gün-içi hatırlatma İKİ AŞAMAYA bölündü** (`eb8198c`, fonksiyon deploy edildi). Kullanıcı talebi: hem sabah "gidecek misin" hem sonrasında "gittin mi", ikisi de Buzz'da. `send-meetup-reminders/index.ts` yeniden yazıldı: **Stage 1 (sabah, 08-11 İstanbul saati)** — `meeting_at` bugün ve henüz `meetup_morning_reminder_sent_at` yoksa, gelecek-zaman kopyasıyla ("Meeting X today?") sadece onay/red — **DB'ye hiçbir şey yazmıyor** (henüz olmamış bir şeyi "gittin mi" diye işaretlemek erken/yanlış olur, bilinçli karar). **Stage 2 (buluşma saatinden sonra, mevcut akış)** — metni düzeltildi, artık gerçekten geçmiş zaman ("Did you meet up with X?" — öncekinde ikisi de "Gidecek misin?" yazıyordu, tutarsızdı). Migration `20260829130000` (`meetup_morning_reminder_sent_at`). `notifications.tsx`'te üçüncü tip + yeni `onMorningYes`/`onMorningNo` handler'ları (checkin_a/b'ye dokunmuyor). **Matches'teki ayrı "Confirmed meetups" check-in girişi tamamen kaldırıldı** (kullanıcı kararı) — check-in artık tek yer: Buzz. **Test edilmedi henüz** — sabah 08-11 arası cron'un kendiliğinden çalışmasını bekliyoruz (yarın).
- **Not — `matches.tsx`'te küçük artık kod:** "Confirmed meetups" bölümünü kaldırınca `acceptedMatches` state/fetch'i ve `checkinBtn`/`checkinBtnText` style'ları artık hiçbir yerde render edilmiyor (fetch hâlâ çalışıyor, sonucu kullanılmıyor) — zararsız ama temizlenebilir, aşağıdaki P2 listesine eklendi.
- ~~🔜🔜 **Home profil kartını Bumble seviyesine çıkar**~~ — YUKARIDA TAMAMLANDI (2026-08-17/18), eski not aşağıda arşiv amaçlı duruyor: `likes` şeması donduruldu (§3). UI adımı: `HingeProfileCard` tek full-screen swipe → **interleave scroll profil** (fotolar + About/Looking-for/Interests/Languages/Location + prompt kartları), her foto & prompt'ta **Note** iskeleti (backend `likes` gelene kadar Buzz Faz A deseni: buton var, kayıt sonra) + X/❤ alt bar; ayrıca Buzz+Chats empty-state = profil güç çubuğu + aktivasyon checklist. `rn-ui-reviewer` pass. Mevcut Home (`app/(tabs)/index.tsx`): tam-ekran foto + `%68` uyum rozeti + isim/yaş + tagline + "Things in common" (etiketler, müzik/film/kitap) + müsaitlik/içki/sigara/konum kartı + X/❤/⭐ aksiyon butonları + "My ideal date / go-to spot / where I hang out" kartları. **Hedef:** Bumble hissi — foto sırası/yerleşimi, yazı tonu, About Me bölümleri, **prompt/soru cevapları**, ve **fotoğrafı/prompt'u beğenip opsiyonel NOT/YORUM yazma** etkileşimi. **KULLANICI YENİ OTURUMDA BUMBLE EKRAN GÖRÜNTÜLERİNİ ATACAK** — tam bir Bumble profilinin tüm fotoları (yazı tonu, foto yerleri, about, prompt konuşmaları). İş akışı: (1) Bumble görselleri gelsin, (2) `ui-ux-pro-max` skill (görsel sistem) + `rn-ui-reviewer` ajanı (RN implementasyon) + `business-strategist` (hangi özellik "yavaş dating" tezine uyar) devreye, (3) **ÖNCE ürün/scope kararı, SONRA UI**. **KRİTİK MİMARİ BAĞ:** "fotoyu/prompt'u beğen + yorum yaz" = Bumble like-with-comment = **`likes` tablosu = Buzz Faz B ile AYNI veri modeli**. `likes` şemasını (hedef foto/prompt id + opsiyonel yorum metni + liker/likee) **bir kez** doğru tasarla; bu şema hem Home like-etkileşimini hem Buzz "Unlock → seni kim beğendi (+ hangi fotona ne yazdı)" ekranını besler. Yani Home redesign ile [[Alerts Faz B]] birlikte tasarlanacak.
  - ~~**Alt-kalem — empty/0-state'ler**~~ — YUKARIDA TAMAMLANDI (`9509ad2`, UI-3). Not: asıl çözüm UI değil **likidite/growth** (beta'yı tek şehre sıkıştır, `business-strategist`); empty-state yara bandı.
- ✅ **`likes` WRITE side (Home) BAĞLANDI (2026-08-16).** Home ❤ (target_type='profile') + foto/prompt **Note** (target_type='photo'|'prompt' + opsiyonel note) tek `recordLike` upsert'i (`onConflict liker_id,likee_id` → kişi başına tek satır). İkisi de günlük kotayı düşürür + feed ilerletir (Note artık skeleton değil; ❤ eskiden **hiçbir şey kaydetmiyordu** — sadece sayaç). Ölü ⭐ SuperSwipe **gizlendi** (yalnız ✕/❤). Migration `supabase/migrations/20260816120000_create_likes.sql` — **UYGULA.**
- ~~🔜 **Alerts Faz B — READ side**~~ — YUKARIDA TAMAMLANDI (`get_my_likers` RPC, `a7a448d`).
- **P2 Katman 2 — gerçek foto gönderimi (DB'ye dokunur, büyük iş):** `messages.media_url` kolonu (+ `content` nullable), özel storage bucket + RLS (accepted-match kontrolü), `renderMessage` foto balonu, signed URL upload. `openPhotoPicker` içinde `TODO(P2 Katman 2)` işaretli. Önce **ürün/KVKK kararı** (foto akışı "yavaş dating" tezine uyuyor mu, moderasyon). `supabase-expert` ajanını hak eder. **Ses notu bilinçli olarak EKLENMEYECEK** (dwell-time artırır, buluşma tezinin tersi).
- **Kimlik güvenilirliği — ertelendi, büyük iş (2026-09-01, kullanıcı kararı: "büyük iş, şimdilik gerek yok, ileriye kalsın").** (1) **Telefon doğrulama** — `phone_number` tamamen self-reported, OTP ile doğrulanmıyor; Supabase Auth'un native phone OTP desteği var, sahte hesabın en ucuz engeli bu. (2) **Foto doğrulama gerçek değil** — `photo_verified` kolonu sadece render edilen bir bayrak, arkasında selfie-liveness↔profil-fotoları karşılaştırma akışı yok. Küçük kullanıcı sayısında otomasyona gerek yok, elle (Doğukan) onaylanabilir; ölçekte otomatikleşir.
- Tema token seti **kısmen** uygulandı (token'lar eklendi ama sadece chat input'ta kullanıldı; diğer ekranlar hâlâ gri/accent hardcode ediyor — token sweep açık). ErrorState retry butonu ~42pt <44pt hâlâ açık.
- ölü kod/seed temizliği; analytics + beta.

**✅ 2026-09-01 — güvenlik aşaması devam: PII log temizliği + reports artık sonuçsuz kalmıyor + gerçek ölü kod tespiti.** PII debug log'ları (`profile-setup/step1-4.tsx`, `register.tsx` — isim/DOB/telefon/konum basan `console.log`'lar) temizlendi (`fc96226`). `reports` tablosu write-only bir kara delikti (admin paneli yok, hiçbir yerde okunmuyordu) — yeni trigger `auto_hide_after_reports`: bir kullanıcı **3 farklı** kişiden (distinct `reporter_id`, aynı kişiden tekrar değil — kötüye kullanımı engellemek için) rapor alınca `profiles.is_hidden` otomatik `true` oluyor, mevcut is_hidden-RLS zaten yeni keşiften çıkarıyor, chat/match bozulmuyor (migration `20260901090000`, `e3d0e9c`, canlıda doğrulandı). Ayrıca önceki oturumdaki "5 ölü DB fonksiyonu" tespiti **yanlış çıktı** — detay aşağıdaki P2 notunda, sadece gerçekten ölü olan `send_like` silindi (migration `20260901093000`, `1ad5a15`).

**✅ P0 + P1 TAMAMEN KAPANDI (2026-08-30/31, denetim 2026-08-29'da yapılmıştı).** Tek tek: şifremi unuttum akışı kuruldu (`forgot-password.tsx` + `authDeepLinks.ts` recovery-tipi ayrımı + `_layout.tsx` otomatik `/change-password` yönlendirmesi + `settings.tsx`'e nav satırı, `4a3a1b1`, cihazda uçtan uca doğrulandı); `user-profile.tsx`/`map.tsx` block/report artık hata gösteriyor + `vibeCategories.ts` `fetchBlockedUserIds` artık fail-closed (hata fırlatıyor, sessizce boş dönmüyor) + bunun kırdığı `vibe-detail.tsx`'e (hiç error-state'i yoktu) yenisi eklendi (`3960f9e`); `checkin.tsx` yazmaları artık hata gösteriyor, Home ❤/Note artık sadece yazma başarılıysa ilerliyor/kota düşürüyor, `(tabs)/profile.tsx`'e ErrorState eklendi (`1cff4f9`); `matches.tsx` sahte "28 yaş" kaldırıldı + `handlePass` hata kontrolü + `premium.tsx` placeholder metni + yeni `blocked-users.tsx` ekranı (`8bf0ee1`); ölü `app/onboarding/*` silindi (`ea7ff6b`); `dailyViews.ts` artık atomik RPC (`increment_daily_views`, satır kilidi — `try_send_invite`'ın deseni, migration `20260831090000`, `2d1f1da`); `unreadMessageCount.ts` artık gerçek `messages.read_at`'e bakıyor (migration `20260831100000`), `chat.tsx` mesajları okundu işaretliyor, `messages.tsx` her ziyarette rozeti tazeliyor (`96325eb`) — bu son ikisi cihazda doğrulandı. Ayrıca kullanıcı isteğiyle **Profile sekmesi alt bara eklendi** (Chats'in sağı, `96325eb`).
- **Kalan P2 (kod kalitesi/temizlik, aciliyeti düşük):**
  - `safeAge` hâlâ birkaç dosyada kopyalanmış duruyor (bugünkü profil.tsx rewrite bunlardan birini elemiş oldu, `HingeProfileCard`'ın `hingeSafeAge`'i kullanıyor artık).
  - `_layout.tsx`'te var olmayan dosyalara işaret eden `Stack.Screen` kayıtları: `coming-soon`, `match-results`, `modal` (`reset-password` gerçek `forgot-password`'a çevrildi, kapandı).
  - ~~`profile-setup/step1-4.tsx` + `register.tsx` PII debug log'ları~~ — KAPANDI (2026-09-01, `fc96226`).
  - Erişilebilirlik etiketleri app genelinde hâlâ seyrek (bugün dokunduğum yerlere eklendi: step1 konum butonu, profile-edit tüm butonlar, Home block/report/pass/like, profile.tsx edit/sign-out — ama geneli kapsamıyor).
  - ~~DB'de 5 ölü fonksiyon~~ — **DÜZELTME (2026-09-01):** önceki tespit yanlıştı (`information_schema.triggers` boş dönüyordu, `pg_trigger`'a bakınca gerçek tablo görüldü). `handle_match_notification`/`handle_mutual_accept`/`update_match_status`/`update_updated_at` aslında **canlı ve trigger'a bağlı** (`match_notification_trigger`, `trg_mutual_accept`, `trg_match_mutual`, `profiles_updated_at`) — silinmedi. Eski `user_a_accepted`/`user_b_accepted` akışından kalmalar (bugünkü `acceptMatchInvite` bu kolonları hiç set etmiyor, o yüzden `handle_mutual_accept`/`update_match_status` tetikleniyor ama fiilen no-op) — zararsız, dokunulmadı. **Yeni bulunan konu:** `handle_match_notification`, her yeni `pending` match satırında (Home/Matches'in arka planda ürettiği keşif kartları dahil) karşı tarafa otomatik "Yeni bir eşleşmen var!" (`new_match`) bildirimi yazıyor — `notifications.tsx`'te `LIKE_TYPES` içinde olduğu için Buzz feed'inden zaten gizleniyor (kimse görmüyor) ama DB'de gereksiz satır birikiyor. Düşük öncelik, ayrıca ele alınabilir. **Gerçekten ölü olan tek fonksiyon `send_like`'tı** — silindi (`1ad5a15`, eski `likes.liked_id` kolonunu referans veriyordu, bugünkü şema `likee_id` kullanıyor, çağrılsa hata verirdi). `matches.confirmed_slot` (eski text kolon) hâlâ dead, kod kullanmıyor. `matches.updated_at` diye bir kolon **yok** (yanlış hatırlanmış, sadece `profiles.updated_at` var ve doğru çalışıyor). `supabase/migrations/20260622100100_get_top_matches_is_hidden_filter.sql` hâlâ yanıltıcı duruyor (uygulanmamış), temizlenebilir. `matches.tsx`'teki `acceptedMatches`/`checkinBtn` hâlâ render edilmiyor.
- **Yarın sabah test edilecek:** iki-aşamalı buluşma hatırlatmasının sabah (08-11 İstanbul) kısmı — gerçek cron'un kendiliğinden ateşlenmesini bekliyoruz, henüz uçtan uca doğrulanmadı.

**✅ Onboarding tek-soru-tek-ekran restructure TAMAMLANDI (2026-09-02/03, `00e3285`, önceki iki commit `60718d5`+`df46109` ile birlikte).** Kullanıcı isteği: tüm dating app'ler (Hinge/Raya referans ekran görüntüleri) tek uzun form yerine tek-soru-tek-ekran ilerliyor — **step1'den step4'e TÜM `profile-setup` akışı** (33 alan) bu desene çevrildi, sadece step1 değil:
- **step1 (9 ekran)** `app/profile-setup/step1/{index(telefon),photos,verify(selfie),name,instagram,birthdate,location,gender,languages}.tsx` — telefon → fotolar (6 slot, otomatik ilk-boş-slota-atama) → selfie doğrulama → ad-soyad → instagram (opsiyonel) → doğum tarihi (native wheel picker) → konum → cinsiyet+buluşma tercihi → diller (`submitAll()`).
  - **Konum ekranı Raya-tarzı autocomplete'e geçirildi** (`lib/turkishGeo.ts`, yeni dosya): şehir + ilçe artık chip/serbest-metin değil, yazarak-ara + listeden seç ("is" → "Istanbul, Turkey", "kadi" → "Kadıköy, Istanbul") — 5 şehir için gerçek ilçe listeleri (Türkçe diyakritikli, kanonik) gömülü, ücretsiz/yerel (paid geocoding API kullanılmadı, kullanıcı reddetmişti). Bu, **"Kadikoy" yazan ile "Kadıköy" yazan artık aynı kanonik değere düşüyor** demek — `get_top_matches`'in `discovery_max_distance` filtresini (aşağıda) daha güvenilir kılıyor. Ayrıca **mesafe tercihi chip'leri** eklendi (`DISCOVERY_DISTANCE_OPTIONS`, `lib/profileSettings.ts`'ten).
- **step2 (4 ekran)** `app/profile-setup/step2/{index,q1,q2,q3}.tsx` — niyet seçimi → niyete göre değişen 3 takip sorusu (`lib/onboardingStep2Context.tsx`'te `QUESTIONS_BY_INTENT`). Eski "not sure yet" sparkle+tooltip kaldırıldı (yeni sade tek-soru estetiğine uymuyordu).
- **step3 (6 ekran)** `app/profile-setup/step3/{index,recharge,hobbies,drinking,education,religion}.tsx` — morning/night → recharge style → hobiler (öneri dropdown'lu, max 5) → içki/sigara → eğitim (+ opsiyonel detay) → inanç. Tamamı opsiyonel — Next hiç kilitlenmiyor, eski global "Skip for now" butonuna gerek kalmadı (bir şey seçmeden geçmek aynı etkiyi veriyor).
- **step4 (6 ekran)** `app/profile-setup/step4/{index,hours,venue,neighborhoods,firstmeeting,bio}.tsx` — müsaitlik günleri (Always stagger animasyonu korundu) → saatler → buluşma mekanı tercihi (+ seçilen her mekan için opsiyonel favori-mekan alanı) → mahalleler (öneri dropdown'lu, max 3) → ilk buluşma beklentisi → bio (300 karakter). Son ekran **"Complete Profile"** — `setup_completed=true` yazıp `replace()` ile uygulamaya giriyor (bilinçli `replace`, geri dönülmesin diye — diğer tüm adım geçişleri `push()`).

**Ortak altyapı:** her step kendi Context'i (`onboardingStep{1-4}Context.tsx`, aynı `Step{N}Provider` deseni — auth-check + state + `submitAll()`) + paylaşılan `components/ui/QuestionScreen.tsx` şablonu (üstte ilerleme çubuğu, altta yuvarlak Previous/Next butonları, `macroStep` prop'uyla hangi step2/3/4'e ait olduğunu bilir). **Renk kararı (sadece bu akış):** app'in altın accent'i TAMAMEN kaldırıldı, Raya'nın siyah/monokrom stiline geçildi (`#1A1A1A` — `Chip`'e yeni `selectedColor` prop'u) — geri kalan app'in altın kimliğine dokunulmadı.

**Navigasyon düzeltmeleri (kritik, cihazda bulundu):** step'ler arası geçiş `router.replace()`'ten `push()`'a çevrildi — `replace` tüm geçmişi siliyordu, geri gidilecek bir yer bırakmıyordu (`"GO_BACK was not handled by any navigator"` hatası + step1'de verilen cevapların kaybolması). `router.canGoBack()` koruması her geri butonuna eklendi (Home'un `current_step`'e göre `replace()` ile doğrudan ortadaki bir step'e attığı durumda geri butonu güvenle gizleniyor). `profile-setup/_layout.tsx` + her step'in kendi `_layout.tsx`'indeki Stack'e `gestureEnabled`+`fullScreenGestureEnabled` eklendi — swipe-back artık tüm akışta (hem step'in kendi ekranları arasında hem step'ler arası) çalışıyor. Artık ölü olan `SetupScreenHeader.tsx` silindi (step2-4 artık hepsi `QuestionScreen` kullanıyor).

**Test kolaylığı — `components/ui/DevStepNav.tsx` (yeni, `__DEV__`-gated, üretimde hiç render olmuyor):** sağ üstte küçük **1 2 3 4** — hangisine dokunursan o step'e atlıyorsun (aktif renkte — artık siyah, bkz. aşağıdaki genel siyah-kimlik geçişi) + kırmızı **⎋** çıkış butonu (bayat/yenilenemeyen bir oturuma takılınca hızlı sign-out+login için). Kalıcı bir özellik değil, onboarding stabilize olunca kaldırılabilir.

**Yol boyunca bulunan/düzeltilen 2 ayrı altyapı bugı (onboarding dışı, genel etkili):**
1. **Supabase RN auto-refresh eksikti** (`fix(auth)`, `df46109`) — `autoRefreshToken:true` tek başına yetmiyor, RN arka planda JS timer'larını durduruyor; Supabase'in resmi RN kılavuzunun istediği `AppState` üzerinden `startAutoRefresh`/`stopAutoRefresh` hiç yoktu. Belirti: gece arka planda kalan bir oturum sabah ilk yazma isteğinde (foto upload) `"new row violates row-level security policy"` ile patlıyordu — RLS policy doğruydu, token bayattı. `app/_layout.tsx`'e `AuthRefreshBridge` eklendi.
2. Foto slotları tıklanan yere değil ilk boş slota atanmıyordu; district/city dropdown'u altındaki içerikle z-index çakışıyordu (blok bazlı z-index sıralaması düzeltildi); seçim sonrası klavye otomatik kapanmıyordu (`Keyboard.dismiss()` eklendi).

`register.tsx` sadeleştirildi (sadece email+şifre; telefon step1'in kendi ekranına taşındı, `lib/countries.ts`'e çıkarıldı).

Cihazda step1→2→3→4→uygulama uçtan uca test edildi, kullanıcı onayladı.

**✅ App genelinde Bumble/Raya tarzı siyah kimliğe geçiş + profil kartı cilası (2026-09-03/04, `f2d754f`).** Onboarding'de kalan gold→black kararı kullanıcı tarafından **tüm uygulamaya genişletildi** ("sarı ile olan kısımları da... raya ile yazdığımız stil ve renklerle yapabilir miyiz"):
- `colors.accent` (`lib/designTokens.ts`) `#B8860B` → `#1A1A1A`. Token'ı kullanmayan ~19 dosyadaki hardcode altın hex (çoğu `const ACCENT = '#B8860B'` deseni) tek tek bulunup değiştirildi — **ilk taramada `constants/theme.ts` (ayrı, unutulan bir renk dosyası) kaçırıldı**, ikinci turda bulunup düzeltildi (ders: gold-hex taraması `app/`+`components/`+`lib/` değil proje kökü genelinde yapılmalıymış). Kontrast gereken yerler ayrıca elden geçirildi (`HomeTopIcon`'ın koyu kare zemini üstündeki ikon beyaza çevrildi — hem ikon hem zemin siyah olunca görünmez olurdu; `themed-text.tsx`'in "link" stili artık normal metinle aynı renk olduğu için alt çizgi eklendi).
- Sağ üstteki fazlalık Profile ikonu (Premium/Settings yanında, Profile zaten alt barda) kaldırıldı.
- **`HingeProfileCard`** (Home/Matches/user-profile/kendi Profile — hepsi paylaşıyor): ana fotoğraf artık Bumble gibi sade — üstündeki "Looking for..." caption'ı kaldırıldı (bilgi zaten ayrı "I'm looking for" kartında duruyordu, tekrardı). İsim/yaş yazısının üstü **`lineHeight` tanımsız olduğu için kesiliyordu** (fontSize 28 iken ThemedText'in miras kalan lineHeight'ı 24'tü) — düzeltildi + boyut hafif küçültüldü. About-me chip'leri daha kompakt (padding/font küçültüldü). **"My interests" artık renkli, ilgiye özel emoji kullanıyor** (`lib/hingeProfile.ts`: `ProfileChip.emoji` alanı + `hobbyChipEmoji()` — Gym→💪, Sailing→⛵, Whisky→🥃 tarzı; About-me/Looking-for/Languages hâlâ sade vektör ikon, bilinçli fark).
- **Alt tab bar aktif sekme davranışı** — iki iterasyon gerekti: ilk denemede koyu "hap" (pill) zemin + beyaz ikon eklendi ama bu tab bar'ın sabit yüksekliğini zorlayıp **altındaki etiket yazısını kaybettiriyordu** (kullanıcı ekran görüntüsüyle bildirdi). Kullanıcının gerçek Bumble referansına göre düzeltildi: bar **beyaz kalıyor**, aktif sekmenin ikonu dolu/solid şekle geçiyor, ikon+yazı **siyah+kalın** oluyor, ikisi de birkaç piksel **yukarı** kayıyor (`components/ui/ActiveTabIcon.tsx` yeni, `TabBarBadgedIcon.tsx`'e `focused` prop, `(tabs)/_layout.tsx`'e özel `tabBarLabel`).
- **Karanlık mod bug'ı bulundu/kapatıldı** — `app/_layout.tsx` sistem temasını takip ediyordu (`colorScheme==='dark' ? DarkTheme : DefaultTheme`); uygulamanın gerçek bir karanlık mod tasarımı hiç yok (her ekran hep açık palet kullanıyor) — gece testinde telefon otomatik karanlık moda geçince sadece navigasyon çerçevesi (tab bar, header) koyulaştı, geri kalan her ekran beyaz kaldı, yarı-siyah-yarı-beyaz kırık bir görünüm oluştu. **Fix: uygulama artık her zaman `DefaultTheme`** (açık), `useColorScheme`/`DarkTheme` importları kaldırıldı.
- Home'da Block/Report altındaki ~144px'lik gereksiz boşluk kırpıldı (`scrollContent`+`footerActions`'ın `paddingBottom`'ları üst üste biniyordu — 120+24 yerine 24+24).
- **Her ekranın üstündeki fazlalık Home ikonu (`HomeTopIcon`) tamamen kaldırıldı** (profile-edit, matches, blocked-users, user-profile, kendi Profile, Buzz, login, forgot-password, register — 9 dosya). Çoğunda zaten native header/alt tab bar/kendi geri oku vardı (fazlalıktı), ama **`user-profile.tsx`'te** (`headerShown:false`, başka hiç nav kontrolü yoktu) tek geri-dönüş yoluydu — kaldırılınca ekranda hiç kontrol kalmasın diye yerine gerçek bir `TopBackButton` (geri ok) eklendi. Yol boyunca bulunan bug: **`(tabs)/profile.tsx`'in loading dalında ikon kaldırılınca ekran resmen boş kalıyordu** (spinner bile yoktu, sadece HomeTopIcon vardı) — düzgün bir `ActivityIndicator` eklendi. `HomeTopIcon.tsx` artık hiçbir yerde kullanılmadığı için silindi (bu arada dosyanın kendi eski, ilgisiz bir typed-route tsc hatası da onunla birlikte gitti).

Cihazda tüm gün boyunca (gündüz + gece karanlık-mod testi dahil) adım adım test edildi, her tur kullanıcı onayı sonrası bir sonrakine geçildi.

**✅ Matches performansı + navigasyon restructure (2026-09-06/07, `ae919f7`+`eade696`).**
- **Matches sekmesi yavaşlığı** (kullanıcı raporu: "diğer sekmelere göre çok yavaş dolduyor") — kök neden art arda (paralel değil sırayla) ~9-18 ağ isteğiydi, özellikle eksik-aday-doldurma döngüsü her aday için 2 sorguyu sırayla atıyordu. Birbirine bağımlı olmayan sorgular paralel gruplara alındı (bkz. `app/(tabs)/matches.tsx` üstündeki yorumlar) — sıralı aşama sayısı 4'e indi. **Hâlâ MVP öncesi daha da hızlandırma gerekebilir** — bir sonraki adım round-trip sayısı değil, `get_top_matches` RPC'sinin kendi sunucu-taraf çalışma süresi olabilir (`EXPLAIN ANALYZE` ile bakılmalı).
- **Bottom bar 5→4 sekmeye indi: Home, Matches, Buzz, Chats.** Profile artık bottom bar'da değil — her ekranın sağ üstündeki avatar ikonundan açılıyor (Bumble/Hinge referansı: Profile "sürekli kontrol edilen feed" değil, bottom bar'da eşit ağırlık almamalı). Global header'daki Premium tacı da kaldırıldı (ne Bumble ne Hinge kalıcı bir premium ikonu tutuyor) — **Premium artık Profile ekranı içinde bir banner** ("Go Premium ✨" + Explore, app'in siyah kimliğiyle).
- **Profile ekranındayken üstte Filters + Settings ikonları var** (kendi kendine dönen bir Profile ikonu yerine — Hinge'deki gibi); **Settings artık Profile'ın içinde** (alttaki fazlalık Settings butonu kaldırıldı, sadece Edit profile + Sign Out kaldı).
- **Yeni `filters.tsx` ekranı** — `settings.tsx`'teki "Discovery preferences" (kimi görmek istediğin, yaş aralığı, mesafe) oradan çıkarıldı + **şehir** ve **ilişki tarzı (intent)** tercihi eklendi (önceden bunlar SADECE onboarding'de düzenlenebiliyordu, sonradan değiştirilemiyordu — kullanıcı isteği: "filters daha dolu olabilir mesela şehir değiştirebiliriz veya aradığın ilişki tarzını").
- **Yol boyunca bulunan/düzeltilen buglar (bazıları önceden beri vardı, bu turda fark edildi):**
  1. Profile ekranı `headerTransparent:true` kullanıyordu ama içerik alanı header için ayrı yer ayırmıyordu — yeni eklenen Premium banner header'ın üzerine biniyordu. Normal opak header'a çevrildi.
  2. `settings.tsx`/`filters.tsx`/`change-password.tsx`/`blocked-users.tsx`'te aynı hata: `ScreenContainer`'ın güvenli-alan `paddingTop`'u sabit bir `paddingTop:8` ile eziliyordu, "Back" durum çubuğunun altında/üstünde kalıyordu (Haziran'dan beri vardı, kimse fark etmemiş).
  3. Settings'te uzun değerler (email gibi) çirkin iki satıra bölünüyordu (`rowValue`'da `numberOfLines` yoktu) — artık tek satırda kısaltılıyor.

**✅ Home hero foto tam ekran + Bumble rozetleri + `occupation` alanı (2026-09-07, `c83d9fe`).** Kullanıcı Bumble ekran görüntüleri referans verdi: ana foto artık kenardan kenara, ekranın ~%88'i (sadece Home'da — `HingeProfileCard`'a `heroFullScreen` prop, Matches/user-profile/kendi Profile eski kart görünümünde kaldı); skor rozeti + "Photo verified" pill'i düz opak siyahtan yarı-saydam koyu+beyaz yazıya döndü ("çok siyah" geliyordu); isim altına doluysa 💼 iş / 🎓 üniversite satırları eklendi. **Yeni `profiles.occupation` kolonu** — "iş" için hiç alan yoktu, eklendi + Edit Profile'a gerçek input olarak kondu + onboarding step3'e ayrı ekran olarak eklendi (education'dan hemen sonra, religion'dan önce — Instagram'la aynı ekrana **konulmadı**, kullanıcı "ayrı şeyler" dedi; step3 6→7 ekrana çıktı). `first_name` ile ilerlemenin riski burada da çıktı: hangi "Melis" ID'sinin ekranda göründüğü belirlenemedi (10 tane var), demo veri (occupation+photo_verified) hepsine yazıldı.

**✅ `get_top_matches` RPC performansı — ~26x hızlandı (2026-09-08, `5c42ab0`, migration `20260908090000`+`20260908090100`).** Matches sekmesi client-tarafı paralelleştirmeden sonra kullanıcı "hâlâ MVP öncesi biraz daha hızlandırmak gerekecek" dedi — `EXPLAIN ANALYZE` ile RPC'nin kendisine bakıldı, gerçek darboğaz orada çıktı:
- **Planlama süresi (187ms) çalıştırma süresinden (62ms) bile uzundu.** Sebep: `LANGUAGE sql` fonksiyon çağıran sorguya inline ediliyor; `raw_score` ifadesi `match_percentage`/`match_category`/`ORDER BY`'da **3 kez yeniden hesaplanıyordu** (aynı dev CASE ifadesi ~28 kez tekrarlanan SubPlan'a dönüşüyordu). Ayrıca `availability_days`/`meeting_environment` örtüşme sayıları eşik kontrolleri (`>=4/>=2/>=1`) için 2-3 kez aynı `unnest()` alt-sorgusuyla tekrar hesaplanıyordu.
- **Fix (skorlama mantığı bire bir aynı, sadece tekrar hesaplama kaldırıldı):** her alt-skor artık ayrı bir sütun olarak bir kez hesaplanıyor; `scored` CTE'si `MATERIALIZED` işaretlendi (downstream'de raw_score'un yeniden inline edilmesini engelliyor).
- **`profiles.city` için normalize edilmiş (`lower(trim(city))`) kısmi expression index eklendi** — city eşleşmesi artık Seq Scan yerine Index Scan (tablo büyüdükçe asıl fark burada görülecek).
- **Sonuç:** planlama 187ms→8ms, çalıştırma 62ms→2ms, toplam ~250ms→~9ms. Bu RPC hem Home'un keşif akışını hem Matches'in eksik-aday-doldurma döngüsünü besliyor, kazanç ikisine de yansıdı. `matches` tablosunun indeksleri zaten yeterliydi (küçük tablo, planner bilinçli olarak Seq Scan seçiyordu — dokunulmadı).
- Yöntem not: **DB perf sorunlarında ilk adım her zaman `EXPLAIN (ANALYZE, BUFFERS)` olmalı** — client-tarafı round-trip sayısını azaltmak (önceki tur) faydalıydı ama asıl büyük kazanç RPC'nin kendi planlama/çalıştırma maliyetindeydi.

**✅ Home'a swipe ile like/dislike eklendi (2026-09-08, `bd70709`).** Önceden sadece alttaki ❤/✕ butonlarıyla mümkündü — "her dating app'te olduğu gibi" kullanıcı isteğiyle sağa/sola kaydırma eklendi. Kart aynı zamanda dikey kaydırılabilir olduğu için (About me vs. aşağıda), `Gesture.Pan()` `activeOffsetX`/`failOffsetY` ile sadece belirgin yatay harekette devreye giriyor, dikey denemede ScrollView'e devrediyor (bu tarz "içinde scroll olan swipe kart" için standart desen). Eşik altında geri yaylanıyor, eşik üstünde ekrandan uçup **aynı** `handleLike`/`handlePass`'i tetikliyor (buton mantığıyla birebir). `react-native-gesture-handler` zaten kuruluydu (react-navigation'ın kendi swipe-back'i için transitive) ama app kökünde `GestureHandlerRootView` hiç yoktu — resmi olarak gerekli, eklendi (native rebuild gerekmedi, saf JS). Bu arada `heroFullScreen`'in kırpma sorunu da düzeltildi: çok uzun kutuya `cover` ile ortalanan foto yüzün üstünü kesip isim overlay'ini ekran dışına düşürüyordu — `contentPosition="top"` + yükseklik %88→%70.

**✅ Home swipe kararı görsel cila (2026-09-08, `0c6abca`).** Tam ekran beyaz X/❤ overlay'i (800ms bekleten, arkada boş beyaz ekran bırakan) kaldırıldı — kullanıcı Bumble ekran görüntüleriyle karşılaştırdı: (1) kartın kendi üzerine binen, sürükleme mesafesiyle beliren soluk/cam görünümlü rozet (`rgba(255,255,255,0.5)`, kenarlıksız, yumuşak gölge, 76pt) — sağdan merkeze kayarak (`translateX` 90→0) aynı anda büyüyor (`scale` 0.5→1), tam ekranın dikey ortasında (sabit üst offset göz/burun hizasına denk geliyordu, düzeltildi); (2) sıradaki kişinin fotoğrafı artık kart sürüklenirken/uçarken arkada gerçekten görünüyor (`nextUser` peek, boş beyaz yerine). Kalp/✕ butonları da artık `flyOffAndDecide` ile aynı uçuş animasyonunu tetikliyor — buton ile swipe artık aynı his. Beğeni yazma hatasında kart geri merkeze yaylanıyor, sessizce kaybolmuyor.

**✅ Güvenlik — anon'a açık SECURITY DEFINER RPC'ler kapatıldı (2026-09-08, `80185e1`, migration `20260908130000`+`20260908131500`).** Supabase advisor taraması (`get_advisors`) gerçek bir istismar ortaya çıkardı: `try_send_invite`/`increment_daily_views` login'siz (`anon`) role'e açık `SECURITY DEFINER` fonksiyonlardı ve `p_user` parametresinin çağıranın kendi `auth.uid()`'i olduğunu hiç doğrulamıyordu — `try_send_invite` ayrıca premium durumunu client'ın gönderdiği `p_is_premium` booleanından alıyordu, DB'ye hiç bakmıyordu. Yani biri login olmadan `/rest/v1/rpc/try_send_invite`'ı `p_is_premium:true` ile çağırıp günlük 1 yerine 3 davet hakkı kazanabiliyordu (monetizasyon bypass), ya da başka bir kullanıcının UUID'siyle çağırıp onun kotasını kasıtlı tüketebilirdi (`increment_daily_views` aynı sorun). Fix: her ikisine `auth.uid() = p_user` kontrolü eklendi; `try_send_invite` artık premium durumunu `profiles.is_premium`'dan okuyor (parametre imzada kaldı — geriye uyumluluk, client kodu değişmedi). `get_my_likers`'ın zaten `auth.uid() is null` kontrolü vardı (ek savunma katmanı olarak anon grant'i yine de kaldırıldı). `auto_hide_after_reports`/`handle_match_notification` saf trigger fonksiyonu (`RETURNS trigger`) — hiç doğrudan-çağrı senaryosu yok, EXECUTE tamamen kaldırıldı (trigger tetiklenmesi ACL'e tabi değil, kırılmadı). **Ders (ikinci migration gerekti):** ilk denemede `REVOKE EXECUTE ... FROM PUBLIC` yazıldı ama gerçek `pg_proc.proacl`'da `anon`/`authenticated`'e **doğrudan** (PUBLIC değil, ayrı ACL girdisi) grant verilmişti — `REVOKE FROM PUBLIC` bunlara dokunmuyor, advisor tekrar taramasında hâlâ açık gördü; `REVOKE ... FROM anon, authenticated` ile düzeltildi, doğrulandı. Ayrıca 6 fonksiyonda sabitlenmemiş `search_path` (advisor WARN, hiçbiri SECURITY DEFINER değildi, düşük risk ama ücretsiz) `ALTER FUNCTION ... SET search_path = public` ile kapatıldı. **Kalan (manuel, SQL ile yapılamaz):** Auth → Password ayarlarında "Leaked password protection" (HaveIBeenPwned kontrolü) hâlâ kapalı, Dashboard'dan elle açılmalı. Kimlik doğrulama (telefon OTP, gerçek foto verify) ayrı ve bilinçli olarak ertelenmiş (§5).

**✅ Karşılıklı beğeni artık anında sohbet açıyor (2026-09-09, `51225cb`, migration `20260909090000`).** Home'daki bağlamlı beğeni (`likes`) ve Matches'teki algoritmik davet funnel'ı bugüne kadar tamamen kopuktu — kullanıcı denedi, "iki kişi birbirini beğenince Matches'te çıkıyor mu" diye sordu, cevap hayır çıktı (hiçbir trigger/kontrol yoktu). `business-strategist` ile pazar araştırması yapıldı (Tinder/Bumble/Hinge/OkCupid/CMB/Breeze/Whim): **hiçbir mainstream dating app karşılıklı beğeniyi ek bir kapıya bağlamıyor** — Bumble'ın kadın-önce kuralı tek istisnaydı, 2025'te kaldırıldı (eşleşmelerin ~%50'sinde hiç sohbet başlamıyordu). Hinge'in kendi mekanizması (%90 eşleşme→sohbet oranı) bizim `likes` şemamızla birebir aynı: bağlamlı beğeni+not, eşleşince not otomatik ilk mesaj oluyor — o model çalındı.
- **Mekanik:** `handle_mutual_like()` trigger (`likes`'a `AFTER INSERT OR UPDATE`) — ikinci beğeni düşünce ters satırı kontrol ediyor, varsa `matches` satırını (var olan algoritmik pending/passed/expired satırı dahil) `status='accepted', chat_opened=true, source='mutual_like'`e yükseltiyor (yeni `matches.source` kolonu, check constraint `algo_invite`/`mutual_like`). `match_score=100` (karşılıklı açık rıza = en güçlü sinyal). `idx_matches_unique_pair` (çift başına tek satır) yüzünden upsert + zaten açık sohbetleri es geçen `WHERE chat_opened IS NOT TRUE` guard'ı ile idempotent. Not yazılmışsa sohbetin ilk mesajı oluyor (`messages`'a direkt insert), yoksa mevcut icebreaker önerileri devreye giriyor. `likes` satırları `status='matched'` + `match_id` ile bağlanıyor. Her iki tarafa `notifications` (`type='mutual_match'`) düşüyor.
- **Bilinçli sınırlar:** günlük davet kotasını (`try_send_invite`) TÜKETMİYOR (kota soğuk-yabancıya-davet'i rasyonluyor, karşılıklı rıza soğuk değil) — hetero "kadın önce açar" kuralı (`shouldOpenChatOnAccept`) bu yola hiç uygulanmıyor (ikisi de eşit rıza verdi). Mevcut algoritmik davet funnel'ına (`try_send_invite`/`acceptMatchInvite`) hiç dokunulmadı — tamamen paralel bir mekanizma, sadece aynı `matches` satır şekline çıkıyor (chat.tsx/messages.tsx zaten `chat_opened=true` olan her satırı genel olarak işliyor, client kod değişikliği gerekmedi). Push bildirimi bilinçli olarak YOK (Edge Function sadece UPDATE event dinliyor, `invited_by`'a dayalı — mutual-like'ta `invited_by` yok, ayrı iş). Chat-içi "Buluşma öner" çubuğu da ayrı, sonraki bir iş (funnel'ın buluşma-öncelikli tezini bu yolda da korumak için önerildi, henüz kurulmadı).
- `notifications.tsx`'e 3 satırlık ekleme (`typeIcon`/`feedRowText`/`routeForType`) — `mutual_match` tipi olmadan generic default'a düşüp "New notification" gösteriyordu (feedRowText'in default case'i `item.text`'i hiç kullanmıyor, tamamen tip-bazlı sentezliyor — bunu bu turda keşfettik). Şimdi "You matched with X! Say hi 👋" + tıklayınca direkt `/chat`'e gidiyor (`invite_accepted` ile aynı route deseni).
- **Doğrulama:** canlıda SQL ile iki temiz seed profille (mevcut matches/likes'ı olmayan) karşılıklı beğeni simüle edildi — match doğru satırla açıldı, `likes` bağlandı, not ilk mesaj oldu, iki bildirim düştü; idempotency testi (aynı satırı tekrar tetikle → mükerrer kayıt yok) geçti; test verisi temizlendi. Advisor: yeni fonksiyon anon/authenticated'a tamamen kapalı (dünkü güvenlik sertleştirmesiyle aynı desen). **Cihazda de doğrulandı (2026-09-09)** — Doğukan'ın gerçek test hesabı Home'da "Elif" seed profilini beğendi, Elif'e SQL ile geri beğeni eklendi, mutual match anında Chats'te çıktı. Yol boyunca bulunan/düzeltilen 2 ayrı bug: (1) test için yazılan placeholder not ("Hey! Seni de beğendim") gerçek sohbette ilk mesaj olarak göründü — bu bir bug değil, tasarım gereği çalışıyordu (not→ilk mesaj), sadece test metni kötüydü, silindi; (2) Home'da kart arkaplanının opak olmaması nedeniyle kısa profillerde (az chip/foto) `nextCardPeek` arka plan fotosu her zaman sızıyordu (sadece swipe sırasında görünmesi gerekirken) — `swipeCard`'a opak arkaplan eklendi (`6406b0d`).
- ✅ **Hamle 2 TAMAMLANDI (2026-09-09, `895fbaa`, migration `20260909100000`).** `get_top_matches`'e "beni beğenmiş" adaylar için +40 bonus eklendi (aday zaten beni beğenmiş ama ben henüz beğenmemişsem — mutual olsaydı zaten aktif match filtresine takılırdı). Canlıda test edildi: bir test adayı en düşük sıradan (%61, listenin sonu) en üste (%88, ilk sıra) fırladı. `likes(liker_id,likee_id)` unique index'i EXISTS sorgusunu zaten destekliyor, yeni index gerekmedi.
- ✅ **Cihazda ikinci kez uçtan uca doğrulandı (2026-09-09, gerçek "Ceren" seed profili) — Buzz/Matches/Chats üçünde de doğru çıktı.** Kullanıcı geri bildirimi sonrası **Buzz'daki `mutual_match` bildirimi featured karta yükseltildi** (`549bed3`) — önceden generic kalp ikonlu compact satırdı, fotoğraf göstermiyordu (foto çözümleme sadece `FEATURED_TYPES` için çalışıyor). Artık `invite_accepted` ile aynı desen: tek büyük tıklanabilir kart, gerçek foto, "You matched with X! Say hi 👋" + "Open chat" pill, okunduktan sonra feed'e demote olmuyor (sohbet zaten Chats'te). Ayrıca bir beğeni mutual'a dönüşünce (`likes.status` `sent`→`matched`) **"You liked X" satırı artık `likesSentRows`'tan düşüyor** (fetch'e `status='sent'` filtresi eklendi) — "You matched with X!" ile aynı bilgiyi iki kez göstermek gereksizdi. "Someone likes you" blur teaser'ı (`get_my_likers`) zaten sadece `status='sent'` sayıyor, mutual olan otomatik düşüyor, ek kod gerekmedi. **Günlük like hakkı da 3→5'e çıkarıldı** (`lib/dailyViews.ts` `DAILY_VIEW_LIMIT`, `2878e6a`) — test sırasında "That's everyone for today" ile karşılaşılınca kullanıcı isteğiyle kalıcı yapıldı.
- ✅ **Break-the-ice → "quick this-or-that" quiz'e çevrildi (2026-09-09/10, `9319fca`+`8d9cf42`).** Eski profil-alanı-kıyaslama icebreaker'ları ("ikimiz de gezginiz, son gittiğin yer nasıldı" gibi) kullanıcıya sığ geldi. `lib/icebreakers.ts` (eski sistem) tamamen silindi, kullanılmayan `matchPercentage` state'i de chat.tsx'ten kaldırıldı. v1 (Seçenek A — iki taraflı senkron mini-oyun "Seçenek B" olarak ertelendi, ayrı/büyük bir iş): `lib/quickIcebreaker.ts`'te **10 soruluk havuz** (kedi/köpek, çay/kahve, bira/şarap, komedi/dram, deniz/dağ + sonradan eklenen pineapple-pizza, texter/caller, planner/spontaneous, sweet/savory, window/aisle-seat — onboarding alanlarıyla (drinking/smoking/morning_night/hobbies) bilinçli örtüşmüyor). **Bir kez cevaplanıyor, kalıcı:** `profiles.quick_icebreaker_answers` (jsonb `{id,choice}[]`, migration `20260910090000`) — ilk boş sohbette `pickRandomQuestions(5)` ile havuzdan rastgele 5'i seçilip soruluyor, tamamlanınca DB'ye yazılıyor; sonraki HER yeni boş sohbette artık 5 soru yerine tek bir "Your icebreaker ✨" chip'i çıkıyor, tıklayınca aynı üretilmiş cümleyi input'a dolduruyor (kullanıcı geri bildirimi: "Ece'ye de Derya'ya da aynı 5 soruyu her seferinde cevaplamama gerek yok"). Zamanlama endişesi (biri cevaplayıp gönderdikten sonra karşı taraf ne zaman cevap verirse versin sorun olur mu) değerlendirildi — v1 tek-taraflı/asenkron olduğu için sorun değil, sadece Seçenek B'de (senkron reveal) önemli olurdu. **Bulunan/düzeltilen bug (`c6f62db`):** cihazda persist hiç çalışmıyordu (Tugce+Aylin sohbetlerinde ayrı ayrı yeni quiz çıktı) — kök sebep §0/DB bölümündeki PostgREST şema önbelleği tuzağıydı, `NOTIFY pgrst, 'reload schema'` ile çözüldü + select/update'e error logging eklendi.
- ✅ **Bumble referanslı Home/Filters/Profile-limit cilası (2026-09-10, `6bf19a8`+`7f038d4`+`fb2b4dd`+`60ee3cd`).** Kullanıcı Bumble ekran görüntüleri verdi, 4 ayrı iş:
  1. **"N likes left today" yazısı → yatay ilerleme çubuğu** (`likesLeftBar`/`likesLeftTrack`/`likesLeftFill`) — kullanıldıkça doluyor, metin artık sadece accessibilityLabel/Value olarak duruyor.
  2. **Home'un header'ına Filters ikonu eklendi** (`HomeHeaderActions`, `TabHeaderActions.tsx`) — önceden Filters sadece Profile ekranının üstünden erişilebiliyordu.
  3. **`filters.tsx` Basic/Advanced iki sekmeye bölündü** + Advanced'a 2 gerçek filtre eklendi: **Verified profiles only** (`photo_verified`) ve **Non-smokers only** (`smoking`) — Bumble'ın height/family-plans gibi bizde hiç toplamadığımız alanları taklit etmek yerine zaten sahip olduğumuz veriye dayanan, gerçekten `get_top_matches`'in WHERE'ine yazılan (etkisiz-switch olmayan) 2 filtre seçildi (`profiles.discovery_verified_only`/`discovery_nonsmokers_only`, migration `20260910110000`+`20260910110100`).
  4. **Günlük like hakkı premium'da 5→10'a çıktı + reklamı yapıldı.** `try_send_invite`'ın mevcut free=1/premium=3 desenini taklit ediyor: `DAILY_VIEW_LIMIT_PREMIUM=10`, `dailyViewLimitFor(isPremium)`. `increment_daily_views` RPC'si artık `is_premium` de döndürüyor (dönüş tipi değiştiği için DROP+CREATE gerekti, grant'ler yeniden verildi — sadece authenticated). `DailyLimitEmptyState`'e "Go Premium ✨ — get up to 10 likes a day instead of 5" kartı eklendi (sadece henüz premium olmayana gösteriliyor, `/premium`'a yönlendiriyor).

  5. ✅ **Profile ekranına Bumble tarzı kimlik bloğu eklendi (`db5e2b8`).** "Pay plan"/"Photo insights" sekmeleri ve Premium/Premium+ karşılaştırma tablosu gerçek ödeme/foto-analizi altyapısı gerektiriyordu, bizde yok — birebir kopyalanmadı. Onun yerine elimizdeki veriyle gerçekten çalışan kısmı alındı: küçük yuvarlak avatar + üstüne binen gerçekten hesaplanan "%X tamamlanma" rozeti (`computeProfileCompletion` — foto sayısı/bio/hobiler/en az bir prompt/müsaitlik/diller/doğrulama, 8 eşit ağırlıklı kontrol) + isim + doğrulanmışsa yeşil tik + "Complete Profile" pill'i, tüm satır Edit Profile'a gidiyor. Mevcut premium banner ve HingeProfileCard showcase'i altında aynen duruyor.

- ✅ **Boy (height) alanı + Advanced filters premium'a kilitlendi + Premium sayfası cilası (2026-09-10, `e330452`+`85ccea9`+`e51db73`, migration `20260910120000`+`20260910120100`).** Kullanıcı üç ayrı şey istedi:
  1. **`profiles.height_cm`** (yeni kolon) — onboarding step3'e 8. (son) ekran olarak eklendi (religion'dan sonra, `TOTAL_SCREENS` 7→8), profile-edit.tsx'e düzenlenebilir alan olarak kondu. **Bilinçli olarak `get_top_matches`'in scoring'ine hiç katılmıyor** (kullanıcı açıkça istedi: "algoritmayı değiştirmesin") — sadece filtre.
  2. **`filters.tsx` Advanced sekmesine "Height range" slider'ı** (140-220cm, age-range ile aynı iki-slider deseni) + `discovery_height_min`/`discovery_height_max` (`get_top_matches`'in WHERE'ine eklendi, raw_score'a dokunmadı — verified/non-smokers ile aynı "etkisiz switch olmasın" prensibi).
  3. **Advanced sekmesinin TAMAMI artık premium'a kilitli** (kullanıcı kararı — Bumble'ın kendi deseni: temel keşfi kısıtlamıyor, sadece daraltmayı kısıtlıyor, düşük riskli bir gelir kolu). Premium olmayan kullanıcı sekmeyi görüyor ama kontroller soluk+`pointerEvents:none`, üstünde kilit ikonlu "Advanced filters are a Premium feature — Unlock Premium" kartı (`/premium`'a gidiyor); premium'da tam etkileşimli. Bu, bir önceki turda serbestçe eklenen Verified-only/Non-smokers-only toggle'larını da geriye dönük kilitliyor.
  4. **`premium.tsx` (zaten var olan karşılaştırma tablosu) güncellendi** — bayat rakamlar ("Daily profiles free:3") gerçek değerlere çekildi (5/10 like), hiçbir yerde gerçekten uygulanmayan sahte satırlar ("Active matches", "Unlimited likes") kaldırıldı, yerine gerçek yeni "Advanced filters" satırı eklendi. Spotlight/SuperSwipe tarzı satın-alınabilir boost kartları **bilinçli eklenmedi** (CLAUDE.md §5'teki "swipe hacmi satışı = para-buluşma değeri kuralını ihlal eder" kararıyla çelişirdi).
  **Not:** aktif test hesabı (`5503c067-...`) `is_premium=true` yapıldı (kullanıcı isteğiyle, kilit açık halini görmek için) — Doğukan'ın `e5426159-...` hesabı zaten kalıcı premium.

- ✅ **Advanced filters'a zodiac + pets eklendi (2026-09-10/11, `f49c513`+`9d91e8e`, migration `20260910130000`+`20260910130100`).** Height'in devamı — "başka ne eklenebilir" diye soruldu, zaten toplanıp hiç kullanılmayan alanlar önerildi: **zodiac_sign** (her kullanıcıda `date_of_birth`'ten otomatik hesaplanıyor, `lib/zodiac.ts`, hiç filtrelenmiyordu) ve **pets** — ama pets için ilginç bir keşif oldu: **kolon hiçbir yerde yazılmıyormuş**, sadece `HingeProfileCard`'da varsa gösteriliyordu, tamamen ölü veri. Onboarding step3'e 9. (son) ekran olarak "Do you have any pets?" eklendi (`TOTAL_SCREENS` 8→9, height'ten sonra). `discovery_zodiac_signs`/`discovery_pets` (yeni `text[]` kolonlar) — `get_top_matches`'e height'le aynı desende WHERE-only filtre olarak eklendi (raw_score'a dokunmadı, benim tercihim boş VEYA adayın kendi değeri null ise es geçiyor). Advanced sekmesindeki diğer her şey gibi premium kilidinin içinde. Canlıda test edildi: tek burç (Leo) seçilince RPC sadece Leo döndürdü.

---

## 5. Ürün kararları (log)
- **Exclusivity/niş konumlanma — KARAR VERİLDİ (2026-09-01, `business-strategist` ajanı + kullanıcı sohbeti).** Raya-tarzı kimlik/statü-bazlı seçkincilik (kabul komitesi, sosyoekonomik filtre) **reddedildi** — hem büyük bir başvuru havuzu gerektirir (bugünkü ölçekte yok, likiditeyi öldürür) hem de "gerçek/güvenilir insanlar" pozisyonuyla çelişebilir (statü ≠ güven). Onun yerine **davranışsal + güven-bazlı exclusivity**: "Girmek davetle olur, kalmak buluşmakla." Somut V1 (bu oturumda başlandı):
  1. **Instagram alanı** (self-reported, `instagram_handle`) — hem onboarding'de hem Edit Profile'da. Tam Instagram OAuth/API entegrasyonu YOK (Meta API onay süreci gerektirir, kapsam dışı) — sadece yazdırıp elle foto-doğrulama sırasında göz atmak için.
  2. **Foto doğrulama — elle onay, otomasyon YOK.** Onboarding'in ilk adımında opsiyonel canlı-kamera selfie (`verification_selfie_path`, `user-photos` bucket'ında `{userId}/verification_selfie.jpg`) — galeriden seçtirilmiyor (sahteciliği zorlaştırmak için). `photo_verified` kolonu zaten vardı, sadece render-only bir rozetti; şimdi gerçek bir onay akışının hedefi oldu (Dashboard'dan bakıp elle `true` yapılacak, ayrı bir admin ekranı KURULMADI — ölçekte gereksiz).
  3. **Telefon OTP doğrulama — ERTELENDİ.** Gerçek SMS göndermek için Supabase'in bir SMS sağlayıcısına (Twilio vb.) bağlı olması lazım, bu ücretli/ayrı bir kurulum — kullanıcı henüz açmadı.
  4. **Görünür bekleme listesi (waitlist) — kozmetik, ACCESS GATE DEĞİL.** `profiles.waitlist_number` (kayıt sırasına göre sequence'tan atanıyor) + `waitlist_boost` (doğrulama/davet her biri düşürüyor) — kullanıcı kararı: "sadece görünüş/his, gerçekten geciktirmesin" (likidite riski almamak için bilinçli). Henüz UI'da gösterilmiyor (kolonlar hazır, ekran yapılmadı).
  5. ~~**Doğrulananlara `get_top_matches`'te öncelik**~~ — TAMAMLANDI (2026-09-02, `6736b3b`, migration `20260902090000`). `photo_verified=true` profiller skorlamada +15 bonus alıyor (diğer eşit-ağırlıklı bonuslarla aynı skala). Bugün itibarıyla 502 profilden hiçbiri `photo_verified=true` değil (manuel onay akışı henüz kullanılmadı) — yani şu an sıralamada gözlemlenebilir bir etkisi yok, elle onay başladığında devreye girecek.
  Migration `20260901100000` (kolonlar) canlıda uygulandı. Kod tarafı (Instagram+selfie — bkz. aşağıdaki "onboarding 9-ekran restructure" notu, artık `app/profile-setup/step1/` altında ayrı ekranlar) yazıldı, `tsc` temiz, **ama cihazda henüz uçtan uca test edilmedi — bilinçli olarak commit edilmedi.** Riskli nokta: selfie çekme `expo-image-picker`'ın kamera tarafını ilk kez kullanıyor, `app.json`'da bu paket için açık bir plugin/izin config'i yok (galeri tarafı zaten paketin varsayılanlarıyla çalışıyordu) — kamerada izin ekranı hiç çıkmazsa/çökerse `app.json`'a native config eklenip dev-build yeniden alınması gerekebilir.
- **`discovery_max_distance` artık `get_top_matches`'te gerçek filtre (2026-09-02, `6736b3b`, aynı migration).** Önceden onboarding/settings'te seçilip DB'ye yazılıyordu ama RPC hiç bakmıyordu. Şemada ayrı bir "mahalle" alanı yok — sadece tek bir `district` text kolonu var (`neighborhoods` ARRAY kolonu var ama kod hiçbir yerde yazmıyor/okumuyor, boş/kullanılmayan alan) — bu yüzden `same_neighborhood` ve `same_district` pratikte AYNI filtreye karşılık geliyor (ikisi de: adayın `district`'i benimkiyle Türkçe-diyakritik-normalize eşleşmeli). `whole_city` (ve bu alanı hiç görmemiş eski profiller, default) eski davranış — sadece şehir eşleşmesi, değişmedi. Cihazda henüz test edilmedi (kullanıcı sonraya bıraktı).
- **Mesaj kuralı simetrik kalıyor.** "Kadın accepted olmadan mesaj atabilsin" fikri reddedildi — funnel'ın değeri simetride; muafiyet spam/taciz kapısı açar. Alternatif = funnel'ı hızlandırmak (aksiyonlu "X seni beğendi → tek dokunuşla kabul" bildirimi).
- **`expired` vs `passed` ayrı** (elle geçme ≠ süre dolumu), eleme pencereleri farklı (14 vs 42 gün).
- **Home Bumble redesign + `likes` şeması — KARAR VERİLDİ (2026-08-16, business-strategist+ui-ux-pro-max onaylı).** Bumble özellik seti tezimize göre süzüldü:
  - **ADOPT:** interleave scroll profil (foto↔kart dokusu, tek full-screen swipe DEĞİL) · blok bazlı **"Note"** (bağlamlı beğeni = çekirdek kimlik) · verified rozeti (render-only, `photo_verified` kolonu).
  - **ADAPT (mevcut kolonlardan, yeni tablo YOK):** About-me chip grid · "Looking for" · Interests · Languages · Location · **prompt Q&A** = mevcut serbest-metin kolonları sabit prompt etiketiyle (`favorite_book/movie/music`, `first_date_expectation`, `core_value`, `favorite_activity`).
  - **REJECT-DEFER:** SuperSwipe (swipe hacmi satışı = "parayı buluşma değerine bağla" ihlali).
  - **`likes` şeması DONDURULDU** (bkz §3 — Home Note-beğeni + Buzz Faz B AYNI tablo): kişi başına **tek hedefli beğeni** (şema çok-reaksiyona hazır ama MVP tek); Home Note-beğeni = **ön-sinyal, davet DEĞİL** (kritik: beğeni anında davete dönerse Buzz'ın "seni kim beğendi" verisi kalmaz — funnel: Note-beğeni → karşı taraf Buzz'da görür → davet → kabul → sohbet → buluşma); **not yazmak ÜCRETSİZ, görmek PREMIUM** (asimetri Buzz'da).
  - **Empty/0-state (Buzz+Chats) bu iş kapsamında:** sahte like YOK → **profil güç çubuğu + aktivasyon checklist'i** (foto ekle / prompt cevapla / doğrulan); asıl çözüm likidite.
  - **Tek beğeni modeli (2026-08-16 uygulandı):** Home ❤ + Note tek `recordLike`→`likes` upsert; boş/no-op aksiyon kalmadı. **Önemli tespit:** eski ❤ aslında hiçbir şey kaydetmiyordu (sadece günlük sayaç), yani "Home like → Matches davet" akışı Home'da DEĞİL — davet zaten Matches ekranında başlıyor. Dolayısıyla Home beğenisini "ön-sinyal (davet değil)" yapmak mevcut funnel'ı BOZMAZ; davet adımı Matches'te aynen durur. SuperSwipe ⭐ gizlendi.
- **Açık kararlar:** tez kişilik mi / konum-niyet mi · check-in "yavaş dating" tezi · KVKK hassas veri sınırı (Note serbest-metin = başka kullanıcı hakkında; moderasyon `reports`'a bağlanacak) · `not_sure_yet` ağırlığı · para modeli (bkz. business-strategist ajanı).

---

## 6. Sık kullanılan test SQL'leri
```sql
-- Günlük limitleri sıfırla
UPDATE profiles SET daily_views_count=0, daily_views_reset_at=now(),
  daily_invites_count=0, daily_invites_reset_at=now()
WHERE id='e5426159-0d49-42c5-b79d-0d1b3cdfea9a';
-- Eşleşmeyi dirilt
UPDATE matches SET status='pending', expires_at=now()+interval '24 hours' WHERE id='<matchId>';
-- RLS policy dökümü
SELECT tablename, policyname, cmd, roles, qual::text, with_check::text
FROM pg_policies WHERE schemaname='public' ORDER BY tablename, cmd;
-- matches status constraint
SELECT conname, pg_get_constraintdef(oid) FROM pg_constraint
WHERE conrelid='matches'::regclass AND contype='c';
-- profiles gerçek kolon listesi (select ile diff'lemek için)
SELECT column_name FROM information_schema.columns
WHERE table_name='profiles' AND table_schema='public' ORDER BY column_name;
-- RPC gövdesi (grep göremez)
SELECT pg_get_functiondef('public.get_top_matches'::regproc);
```

---

## 7. Teşhis metodu (işe yaradı, koru)
1. Şikayeti önce grep'le (`--exclude-dir=node_modules --exclude-dir=.git`).
2. Kodda yoksa DB/RPC'yi tara — `pg_get_functiondef` ile çek.
3. Karşılaştırmayı **ID ile** yap, isimle asla (13 Asli).
4. DB'ye yazan değişiklikten önce **constraint/policy** kontrol et.
5. Boş ekran/sessiz hata → `error`'ı logla (var-olmayan kolon tüm select'i null yapar).
6. Bir katmanı elemeden diğerine geçme. Her çalışan parçadan sonra: **test → commit → sıradaki.**
