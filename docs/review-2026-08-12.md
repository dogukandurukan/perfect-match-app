# Subagent Review — 2026-08-12

3 subagent ile yapılan ilk proje taraması. Bulgular önceliğe göre.

## 1. Supabase (supabase-expert)

- 🔴 **`app/user-profile.tsx:138` — `intent` bug.** Kolon `profiles`'ta değil, `onboarding_answers` tablosunda. index/profile/matches ekranları zaten oradan çekiyor. PostgREST 400 → `.single()` boş döner, profil ekranı hiç açılmaz.
  **Fix:** select'ten `intent`'i çıkar; ayrı bir `.from('onboarding_answers')` sorgusuyla çek.
- 🟠 **RLS repoda yok.** Migration'larda hiç `CREATE TABLE` / `POLICY` yok — tüm şema + policy'ler Dashboard'da. `supabase db pull` ile çek; özellikle `blocks`/`reports` insert ve `profiles` select RLS'ini teyit et.
- 🟡 `lib/supabaseClient.ts:6` anon key hardcoded (public, sorun değil ama env'e taşınabilir). `service_role` sızıntısı YOK (seed.ts env kullanıyor ✅).
- 🟢 Yinelenen settings migration: `20260622100000_profiles_settings_columns.sql` vs `20260723000000_add_profile_settings_columns.sql` — zararsız, ikincisi gereksiz.

## 2. UI / UX (rn-ui-reviewer)

Referans: Hinge · accent #B8860B · tokenlar `lib/designTokens.ts`. İncelenen: index, messages, chat, user-profile, matches, vibe, map, notifications.

- 🔴 **Error/retry state HİÇBİR ekranda yok.** RPC hataları sessizce boş listeye dönüşüyor (`index.tsx:135`, `messages.tsx:38`, `notifications.tsx:114`, `map.tsx:206`). Ortak `ErrorState` bileşeni + "Tekrar dene" CTA gerekli.
- 🟠 `chat.tsx:290` — loading yok, "Chat is locked" flaş'ı. `chatOpened===null` iken spinner, kilit ekranını sadece `===false`'ta göster.
- 🟠 `chat.tsx:236` — mesaj gönderme hatası kullanıcıya görünmüyor (sadece metin geri konuyor).
- 🟠 `user-profile.tsx:236` — loading spinner yok; `:149` "profil bulunamadı" durumu boş render.
- 🟡 `messages.tsx:161` düz "Loading…" metni → skeleton (`vibe.tsx`'teki `VibeStripSkeleton` deseni referans).
- 🟡 Empty state'lerde CTA butonu yok (`messages.tsx:164`, `matches.tsx:1082`, `vibe.tsx:196`).
- 🟡 Tema borcu: `ACCENT='#B8860B'` yerel sabiti + gri tonları (`#FAFAFA/#888/#999`...) her ekranda hardcode. `designTokens.ts` sadece 4 renk. `border/textMuted/bgSubtle` + `spacing.*` eklenmeli.

**Durum:** 8 ekranın hiçbiri 3 state'te tam değil. Loading+empty iyi (6 ekran). Error/retry sistemik sıfır — öncelik. En iyi örnek: `vibe.tsx`.

## 3. Push / Dev-Build (expo-debugger)

- ✅ Hazır: `extra.eas.projectId`, `expo-notifications` plugin, `expo-dev-client` kurulu, `_layout.tsx` listener'ları SDK 54 uyumlu, `start` script'i `--dev-client`.
- ❌ **`eas.json` YOK** + **`eas-cli` projede tanımsız.** Dev-build almadan önce şart.
- ⚠️ Expo Go push'u sessizce `null` dönüyor (try/catch yutuyor) — göçün asıl nedeni.
- 🔧 Küçük: `savePushToken` Supabase hata kontrolü yok; plugin'e bildirim `icon`'u eklenebilir.

**Göç checklist'i:**
1. `npm i -D eas-cli` → `eas login` → `eas build:configure` (development profili: `developmentClient: true`, `distribution: internal`)
2. Push credentials: iOS APNs key (`eas credentials`), Android FCM v1
3. `eas build --profile development --platform ios` → cihaza kur → `npm start`
4. Gerçek cihazda token doğrula → `profiles.expo_push_token`'a yazılıyor mu kontrol

## Öncelik sırası (proje planına uygun)
1. `intent` bug (Supabase #1) — tek satırlık gerçek bug
2. Error state audit (UI #1) — ortak `ErrorState` + retry
3. Push → dev-build göçü (`eas.json` + credentials)
4. Tema token seti genişletme (UI borç)
5. RLS'i repoya alma (`supabase db pull`)
