# Handoff — 2026-08-14

## Aşama
Alpha, ~%75–80 MVP. Çekirdek akış çalışıyor (like→davet→kabul→chat). Tek geliştirici, part-time.

## ŞU AN NEREDEYİZ — commit'lenmemiş açık iş
`git status`: 6 dosya modifiye + yeni dosyalar. **Hepsi working tree'de, henüz commit yok.**

### Bitmiş ama commit'lenmemiş
- ✅ **intent bug FIX** — `user-profile.tsx:154` artık `intent`'i ayrı `onboarding_answers` sorgusuyla çekiyor (profiles'tan değil). Çözüldü.
- ✅ **chat avatar bug** — `chat.tsx` +137 satır elden geçmiş (sağ balon avatar / currentUserId akışı). Test edilmeli.
- ✅ **Error state audit başladı** — `components/ErrorState.tsx` (64 satır) oluşturulmuş, 4 tab'a bağlanmış: `index`, `map`, `messages`, `notifications`. (UI review #1 önerisi)
- ✅ **CLAUDE.md** yazılmış (proje tanıtımı, ortak zemin).

### Sıradaki (öncelikle)
1. Bu değişiklikleri **test et → commit** (intent + chat avatar + error state).
2. Kalan ekranlarda error state: `chat.tsx`, `user-profile.tsx`, `matches`, `vibe`.
3. Push → dev-build göçü: `eas.json` + `eas-cli` yok → `eas build:configure`.
4. Tema token borcu: `designTokens.ts` genişlet (`border/textMuted/bgSubtle` + `spacing.*`).
5. RLS'i repoya al: `supabase db pull`.

## Agent'lar (`.claude/agents/`)
| Agent | İş | Model |
|-------|-----|-------|
| `supabase-expert` | DB şema, RLS, migration, sorgu | sonnet |
| `rn-ui-reviewer` | UI/UX audit, loading/empty/error, tema | sonnet |
| `expo-debugger` | build/native/push/dev-build | sonnet |
| `business-strategist` | monetizasyon, büyüme, metrik, MVP önceliklendirme (kod yazmaz) | opus |

Built-in: `claude`, `Explore`, `general-purpose`, `Plan`, `claude-code-guide`, `statusline-setup`.

## Referans dosyalar
- `docs/review-2026-08-12.md` — ilk 3'lü subagent review (detaylı bulgular)
- `CLAUDE.md` — proje ortak zemini

## Mimari kararlar (değişmez)
- Simetrik funnel (cinsiyet-asimetrik mesaj REDDEDİLDİ)
- Hinge tarzı bağlamlı beğeni hedefleniyor (asıl farklılaştırıcı)
- UI referansı: Hinge, accent #B8860B
